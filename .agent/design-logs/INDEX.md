# Design Log Index

Active and pending logs. For completed history, see [ARCHIVE-INDEX.md](ARCHIVE-INDEX.md).

**Total logs:** 248 | **Active:** 150 | **Archived:** 98

## Folder Structure

- `admin-ui/` — Admin UI (36)
- `ai-review/` — AI Review & Classification (45)
- `capital-statements/` — Capital Statements (4)
- `client-portal/` — Client Portal & Questionnaires (14)
- `documents/` — Documents & OneDrive (20)
- `email/` — Email System (22)
- `infrastructure/` — Infrastructure & Workflows (25)
- `reminders/` — Reminder System (17)
- `research/` — Research & Feasibility (7)
- `security/` — Security & Auth (7)

## Active Logs

| # | File | Status | Summary |
|---|------|--------|---------|
| 422 | [422-recent-messages-group-reply-stuck.md](admin-ui/422-recent-messages-group-reply-stuck.md) | COMPLETED — 2026-05-18 | DL-396 follow-up renders `g.messages.slice(1)` inside `.msg-group-older`, so the latest message has no `.msg-row` in the DOM. Group header 💬 button called `showReplyInput(latestNoteId)` which `querySelector('.msg-row[data-note-id=…]')` → `null` → silent `return` (looks "stuck" to user). Fix: new `showGroupReply(clientKey, noteId, reportId)` after `markGroupHandled` — toggles off if a reply zone exists (satisfies user's "double-click closes" follow-up), else auto-expands the group, synthesizes the latest `.msg-row` via existing `_renderMessageRowHtml`, prepends to `.msg-group-older`, and delegates to `showReplyInput` with the synthesized row as `containerEl`. Header onclick swap (`script.js:1255`). ✓ markGroupHandled + 📁 folder-open verified working (no DOM-row dependency). Cache-bust `script.js?v=432→433`. Preserves DL-396 follow-up principle #1 (no snippet duplication while idle — latest row materializes only during active reply). |
| 419 | [419-inbound-large-file-passthrough.md](infrastructure/419-inbound-large-file-passthrough.md) | COMPLETED — 2026-05-17 | DL-416 (classifier base64 fix) shipped+deployed but a re-triggered 32 MB Drive PDF still DLQ'd at 15:13:08Z — OOM happens upstream of the classifier, in `processor.ts:processAttachment` Step 6 `uploadToOneDrive(…32MB ArrayBuffer)`. CF Workers' `fetch()` body buffer + 32 MB body ≈ 64 MB peak per PUT, on top of 8× attachments held in `attachments[]`. Three changes: (1) NEW `uploadLargeFileToOneDrive` helper using MS Graph `createUploadSession` + 5 MiB chunked PUT (5 MiB = 16 × 320 KiB, satisfies MS Graph's fragment-size multiple requirement; 4 MiB is invalid). `uploadToOneDrive` dispatches >5 MB to chunked path. Uses raw `fetch()` against the pre-authed `uploadUrl` (a Bearer header would be rejected). (2) `processor.ts` classify-batch loop early-returns `null` when `attachment.size > MAX_CLASSIFIABLE_BYTES` — skips Anthropic entirely; pending_classifications row still created with `matched_template_id=null`, `matched_doc_name='קובץ גדול — דרוש סיווג ידני'`, `ai_reason='[DL-419] Skipped AI classification (oversize)'`, fully populated `file_url`+`onedrive_item_id`. Office triages via normal DL-412 reassign flow. (3) Eager memory-free: just before Step 8 sets `attachment.content`/`contentToUpload`/`officePdfContent` to empty so V8 GC reclaims before the next attachment's PUT. (4) Cleanup: `MAX_CLASSIFIABLE_BYTES` `export`ed from document-classifier.ts; DL-416's now-unreachable `isOversize` Anthropic placeholder branch removed. Sources: MS Graph createUploadSession docs (320 KiB-multiple fragment requirement), CF Workers best-practices (128 MB cap, body-buffering), MS Graph PowerShell large-file sample. `wrangler deploy --dry-run` clean. CPA-XXX's stuck 32 MB PDF: user manually re-triggered via `email_events` row twice (3:06pm + 3:13pm DLQ); will re-trigger again after this deploys. No backfill of failed/orphan rows (office triages). |
| 416 | [416-classifier-large-pdf-oom-guard.md](infrastructure/416-classifier-large-pdf-oom-guard.md) | COMPLETED — 2026-05-17 (superseded in effect by DL-419) | 2026-05-17 12:16Z inbound DLQ event: a 32 MB Gmail Drive PDF (DL-414 raised the cap to 50 MB on 2026-05-15) hit `inbound-email` queue and failed 3× with `outcome=exceededMemory` (wallTime ~70s) before dead-lettering. Root cause: `api/src/lib/inbound/document-classifier.ts:810` ran `arrayBufferToBase64(attachment.content)` **unconditionally** before the L813 `isLargePdf` size check — for PDFs ≥5 MB the base64 was then *discarded* (text-placeholder branch). Helper itself (L404–411) used per-byte `binary += String.fromCharCode(...)` concat — O(n²) memory in V8. Peak ≈ 32 MB buffer + 32 MB intermediate string + ~43 MB base64 → past the 128 MB per-isolate Workers cap. Fix (one file, ~30 lines): (1) replace helper with 32 KB chunked `String.fromCharCode.apply(null, subarray) + parts.join('')` encoder; (2) add `MAX_CLASSIFIABLE_BYTES = 20 * 1024 * 1024` constant (memory math: ~3.4N peak per attachment → 68 MB at the ceiling); (3) reorder `classifyDocument` dispatch so `isOversize` routes to the existing filename-only `isLargePdf` branch, base64 encode moved inside small-PDF and image branches only. `ClassificationResult` shape, queue config, DLQ handler, Anthropic API call — all unchanged. Verified via `npx wrangler deploy --dry-run` (clean build, no type errors). V4 validation: audit Airtable for `email_events`/`pending_classifications` rows for the DLQ'd `message_id=AAMkAG…AABg2jxOAAA=` — logs suggest a separate MS Graph webhook re-fire at 12:15:38Z already landed it, but confirm before deciding recovery. |
| 420 | [420-never-silently-drop-attachments.md](infrastructure/420-never-silently-drop-attachments.md) | IMPLEMENTED — NEED TESTING — 2026-05-17 | Hard invariant: every inbound attachment must end up with a `pending_classifications` row in AI Review (and a OneDrive upload when bytes were obtained), regardless of classify failure / upload failure / `too_large` Drive rejection / corrupt ZIP. 2026-05-17 incident: 13 attachments → 7 PCs + 1 `too_large` + **5 silent drops**. Office doesn't visit Outlook for orphans — AI Review IS their inbox. Fix bundles: (1) `attachment-utils.ts:fetchDriveAttachment` `too_large` rejects synthesized into the `attachments[]` array as stubs with `tooLarge=true` + `driveUrl` so the per-attachment loop sees them. (2) New `createFallbackPendingClassification()` writes a metadata-only PC with `matched_template_id=null`, `ai_reason='[DL-420] …'`; for `tooLarge` stubs `file_url=driveUrl` so AI Review's open-link button jumps to Drive. (3) `processAttachmentWithClassification` gains `outcome?:{pcCreated}` out-param marked `true` on successful create / duplicate-skip / non-document short-circuit. (4) Per-attachment loop wrapped in try/catch — on throw without `pcCreated`, writes a fallback PC and accumulates `{name,reason}` into `failedAttachments[]`. (5) After loop, PATCH `email_events.attachments_failed_count` + `failed_attachments` (multilineText) with `typecast:true` to auto-create the fields on first failure — `airtable.ts:updateRecord` extended with `opts.typecast`. (6) `admin-stuck-emails.ts` adds a new `partial-failure` bucket for `Completed`-with-failures rows (second formula query wrapped in try/catch — 422s silently until typecast creates the field). (7) DL-417 widget gets a new `❗ כשל חלקי (N)` count pill + per-row `❗ N נכשלו` badge with `failed_attachments` as tooltip, cache-bust `?v=3→v=4`. (8) `archive-expander.ts` untouched — its existing "keep raw ZIP in `attachments[]` when extract fails" behavior is exactly what the new loop body needs. Recovery procedure documented in Section 5 — do NOT re-process the same MS Graph message (`conflictBehavior=rename` would dupe OneDrive files); re-forward only the 6 missing attachments as a fresh email so `file_hash` dedup correctly skips the 7 already-processed ones. |
| 419 | [419-inbound-large-file-passthrough.md](infrastructure/419-inbound-large-file-passthrough.md) | BEING IMPLEMENTED — 2026-05-17 | DL-416 (classifier base64 fix) shipped+deployed but a re-triggered 32 MB Drive PDF still DLQ'd at 15:13:08Z — OOM happens upstream of the classifier, in `processor.ts:processAttachment` Step 6 `uploadToOneDrive(…32MB ArrayBuffer)`. CF Workers' `fetch()` body buffer + 32 MB body ≈ 64 MB peak per PUT, on top of 8× attachments held in `attachments[]`. Three changes: (1) NEW `uploadLargeFileToOneDrive` helper using MS Graph `createUploadSession` + 5 MiB chunked PUT (5 MiB = 16 × 320 KiB, satisfies MS Graph's fragment-size multiple requirement; 4 MiB is invalid). `uploadToOneDrive` dispatches >5 MB to chunked path. Uses raw `fetch()` against the pre-authed `uploadUrl` (a Bearer header would be rejected). (2) `processor.ts` classify-batch loop early-returns `null` when `attachment.size > MAX_CLASSIFIABLE_BYTES` — skips Anthropic entirely; pending_classifications row still created with `matched_template_id=null`, `matched_doc_name='קובץ גדול — דרוש סיווג ידני'`, `ai_reason='[DL-419] Skipped AI classification (oversize)'`, fully populated `file_url`+`onedrive_item_id`. Office triages via normal DL-412 reassign flow. (3) Eager memory-free: just before Step 8 sets `attachment.content`/`contentToUpload`/`officePdfContent` to empty so V8 GC reclaims before the next attachment's PUT. (4) Cleanup: `MAX_CLASSIFIABLE_BYTES` `export`ed from document-classifier.ts; DL-416's now-unreachable `isOversize` Anthropic placeholder branch removed. Sources: MS Graph createUploadSession docs (320 KiB-multiple fragment requirement), CF Workers best-practices (128 MB cap, body-buffering), MS Graph PowerShell large-file sample. `wrangler deploy --dry-run` clean. CPA-XXX's stuck 32 MB PDF: user manually re-triggered via `email_events` row twice (3:06pm + 3:13pm DLQ); will re-trigger again after this deploys. No backfill of failed/orphan rows (office triages). |
| 416 | [416-classifier-large-pdf-oom-guard.md](infrastructure/416-classifier-large-pdf-oom-guard.md) | BEING IMPLEMENTED — 2026-05-17 | 2026-05-17 12:16Z inbound DLQ event: a 32 MB Gmail Drive PDF (DL-414 raised the cap to 50 MB on 2026-05-15) hit `inbound-email` queue and failed 3× with `outcome=exceededMemory` (wallTime ~70s) before dead-lettering. Root cause: `api/src/lib/inbound/document-classifier.ts:810` ran `arrayBufferToBase64(attachment.content)` **unconditionally** before the L813 `isLargePdf` size check — for PDFs ≥5 MB the base64 was then *discarded* (text-placeholder branch). Helper itself (L404–411) used per-byte `binary += String.fromCharCode(...)` concat — O(n²) memory in V8. Peak ≈ 32 MB buffer + 32 MB intermediate string + ~43 MB base64 → past the 128 MB per-isolate Workers cap. Fix (one file, ~30 lines): (1) replace helper with 32 KB chunked `String.fromCharCode.apply(null, subarray) + parts.join('')` encoder; (2) add `MAX_CLASSIFIABLE_BYTES = 20 * 1024 * 1024` constant (memory math: ~3.4N peak per attachment → 68 MB at the ceiling); (3) reorder `classifyDocument` dispatch so `isOversize` routes to the existing filename-only `isLargePdf` branch, base64 encode moved inside small-PDF and image branches only. `ClassificationResult` shape, queue config, DLQ handler, Anthropic API call — all unchanged. Verified via `npx wrangler deploy --dry-run` (clean build, no type errors). V4 validation: audit Airtable for `email_events`/`pending_classifications` rows for the DLQ'd `message_id=AAMkAG…AABg2jxOAAA=` — logs suggest a separate MS Graph webhook re-fire at 12:15:38Z already landed it, but confirm before deciding recovery. |
| 418 | [418-portal-false-submission-flag.md](client-portal/418-portal-false-submission-flag.md) | BEING IMPLEMENTED — DL-418 | Client portal landing (`/webhook/check-existing-submission`) falsely flagged CPA-XXX as "already submitted" because `hasSubmission = stageRank >= 3 \|\| documentCount > 0` treated any received document as proof of questionnaire submission. Docs land via office upload / inbound email / AI classification before the client ever submits — conflating two independent state machines. Affected client gated out of the language picker entirely. Fix: single line in `api/src/routes/submission.ts:82` — drop the `documentCount > 0` clause; stage rank is the SSOT for submission (workflow [02] advances stage on Tally fire). Frontend (`landing.js:130-133`) already trusts `has_submission` boolean from API, so one Worker deploy fixes both AR + CS surfaces. |
| 417 | [417-stuck-email-events-diagnose-and-monitor.md](infrastructure/417-stuck-email-events-diagnose-and-monitor.md) | IMPLEMENTED — NEED TESTING — 2026-05-17 | Diagnose + monitor stuck inbound emails (Airtable `email_events.processing_status != Completed`). Track A: read-only `scripts/dl417-diagnose-stuck-emails.mjs` cross-references each stuck row against Airtable (`reports.client_notes`, linked `pending_classifications`), OneDrive (client folder + "לקוח לא מזוהה"), R2 archive (90d, by `source_message_id`), and emits a PII-safe verdict report to `.agent/audits/`. Track B: new admin route `GET /webhook/admin/stuck-emails` + dashboard widget module (`stuck-emails-widget.js`, ratchet-safe) showing bucket counts (`Failed`/`Detected` = stuck, `NeedsHuman`/`PasswordReply` = action-required, `Bounced`/`Discarded` = terminal-expected) with DL-406 aging-tint reuse. Track B also extends `api/src/lib/inbound/dlq-consumer.ts` to stamp `email_events.processing_status='Failed'` + `error_message` when CF Queues DLQ exhausts retries (fail-open, ack always). No schema changes — all 7 status values + `error_message` already exist post-DL-399. Track C (reprocess endpoint) deferred. |
| 415 | [415-ai-review-reassign-period-propagation.md](ai-review/415-ai-review-reassign-period-propagation.md) | IMPLEMENTED — NEED TESTING — 2026-05-17 | Six bugs in AI-review [H:reassign] + merge dialog flows for T901/T902 contracts, surfaced via live testing on CPA-XXX. DL-397 persists `matched_template_id` + `contract_period` on the CLASSIFICATIONS row, but the period is never propagated to the DOCUMENTS row's `issuer_name` / `document_key` — so OneDrive filenames are right while admin/portal see a generic doc. Bugs: (1) reassign to a generic missing T901/T902 placeholder INSERTs a new row instead of UPDATEing the chosen one (period dropped); (2) "Keep both" branch clones target's old period + appends `— [H:part] N` instead of using the modal's NEW period; (3) `pending_classifications.expected_filename` not regenerated when `matched_template_id` flips on reassign (T501→T902 still shows old broker-cert filename); (4) stub generation creates duplicate generic missing placeholders (2 identical T902 rows) the user can't disambiguate; (5) target-dropdown + merge-dialog title triple-render the period (`…([H:expense]) 01.2025-01.2025 01.2025-01.2025 01.2025-01.2025`); (6) merge dialog fires regardless of period overlap. Fix: shared `applyPeriodSuffixToDocFields(docFields, clsFields)` server helper reusing existing `getRentalPeriodLabel()` (`classifications.ts:877`), hooked into reassign UPDATE (L2124), reassign INSERT/general_doc create (L1939/L1999), and keep_both (L2079-2106). Keep_both strips period from `baseTitle`+`baseKey` and re-applies modal's `contract_period`. `expected_filename` regen at Step 5 PATCH (~L2165) when template changes. New `periodsOverlap()` predicate gates the conflict trigger — non-overlapping periods auto-keep-both with new period. Frontend: single `formatDocOptionLabel(doc)` consumed by both `createDocCombobox` option render (~L3560) and `showApproveConflictDialog` title (~L14402), reading `issuer_name` ONCE. Stub-dedupe pass added to the template-stub generator. |
| 414 | [414-doc-upload-size-limit.md](documents/414-doc-upload-size-limit.md) | COMPLETED — 2026-05-15 | Raise admin doc-manager + inbound Drive-link upload cap from 10 MB / 25 MB → **50 MB**. Office hit the 10 MB cap on a real-world tax document. Four constants flip: `MAX_FILE_SIZE` in `api/src/routes/upload-document.ts:26`, `UPLOAD_MAX_SIZE` + Hebrew toast at `frontend/assets/js/document-manager.js:3103/3123`, `DRIVE_DEFAULT_MAX_BYTES` at `api/src/lib/inbound/attachment-utils.ts:124`. Research-verified: CF Workers Pro body cap is 100 MB; MS Graph single-PUT on Business OneDrive actually allows 250 MB (vs. the 4 MB single-PUT figure in old docs); Exchange Online inbound default ~36 MB so 50 MB is generous. No new endpoints, no schema. Cache-bust `document-manager.html ?v=412→413`. Out of scope: client portal, MS Graph upload sessions (deferred to future DL if 30-50 MB single-PUTs ever flake). |
| 413 | [413-lighthouse-perf-and-a11y.md](admin-ui/413-lighthouse-perf-and-a11y.md) | COMPLETED — 2026-05-12 | Authenticated Lighthouse on admin (2026-05-11) returned Perf 75 / A11y 89 / TBT 740 ms with measured root causes: 9,115 DOM nodes from dual desktop+mobile rendering in `renderClientsTable`/`renderReviewTable` + 2,539 eager-rendered nodes in the hidden Review tab + blocking 596 ms `xlsx.full.min.js` at `index.html:11` + 3 a11y trio (`.stat-pct` 2.52:1, dashboard checkbox missing label, `<span class="no-email-indicator">` aria-prohibited-attr). Fix bundles: (T1/T2) `matchMedia('(max-width:768px)')` one-surface render — desktop OR mobile per viewport, viewport-flip listener re-renders cached rows; (T3) `let reviewMounted=false` flag — `loadDashboard` only caches `reviewQueueData`, first `switchTab('review')` triggers render; (T4) `ensureXLSX()` Promise-wrapped UMD `<script>`-injector loader extracted to new `frontend/admin/js/modules/xlsx-loader.js` (avoids ratchet bump) — `processFile`/`exportReviewToExcel`/`exportToExcel` now `async` and `await` before `XLSX.read`/`XLSX.writeFile`; blocking CDN `<script>` at `index.html:11` deleted; (T5) new `frontend/_headers` with `/admin/*` preconnect to cdnjs.cloudflare.com + Workers API (Cloudflare Early Hints replays as 103); (T6) `.stat-pct` color `--gray-400`→`--gray-600` for ≥4.5:1; (T7) checkbox `aria-label="בחר ${client.name}"` on both desktop and mobile templates; (T8) `role="img"` on `<span class="no-email-indicator">` in `bounce-warning.js` legalizing `aria-label`; (T9) client portal `<h3>`→`<h2>` in `view-documents.html` + `document-manager.html` for sequential heading order. Executed via `/subagent-driven-development` in 2 waves (5 parallel implementers on disjoint write sets, then 1 agent consolidating `index.html`). Cache-bust `script.js?v=425→426`, `style.css?v=386→387`, new `<script src="js/modules/xlsx-loader.js?v=1">`. `script.js` held at 16125-line baseline (blank-line trims offset structural adds). Target gates: Perf ≥85, A11y = 100, TBT < 300 ms, DOM ≤ 6,500. Verification: live walk-through (clients scroll/filter, Review first-switch, Excel import/export, mobile resize) + LH re-run before COMPLETED. |
| 412 | [412-spouse-name-in-doc-name-and-reassign-tabs.md](admin-ui/412-spouse-name-in-doc-name-and-reassign-tabs.md) | COMPLETED — 2026-05-11 | Follow-up to DL-411 surfaced on CPA-XXX (`spouse_name = "משה"`): (1) CLIENT-scoped templates picked from the spouse tab tagged `person='spouse'` but the rendered `issuer_name` / `name_short` carried no spouse marker — patterns lacked `{spouse_name}`. (2) AI Review's reassign picker (`_buildDocTemplatePicker` at `script.js:8075`) had no client/spouse tabs at all; reassign-created docs always defaulted to `person='client'`. Fix: auto-append `" – {spouse_name}"` (en-dash, matching `buildShortName` separator) in `buildDocMeta` (`document-manager.js`), `_paResolveTemplateName` (`script.js`), `buildShortName` (`classification-helpers.ts`), and the reassign + also_match new-doc creation paths in `classifications.ts` — guarded by `!includes(spouseName)` so SPOUSE-scoped templates that already substitute `{spouse_name}` don't double-tag. Picker tabs added inside `renderPick` with closure-local `activePerson`, threaded through `pickTemplate` / `confirmVars` / `submitCustom` and onward via `submitAIReassign` (new 9th `person` arg) → `body.person` on `/webhook/review-classification`. `also_match` mirrors via `additional_targets[].person`. `buildShortName` signature gains optional `person`/`spouseName` params; `memoShortName` keys by all four. Worker docs fetch field list gains `person`. Per-request spouse-name lookup cache in `classifications.ts` avoids repeat report fetches. Cache-bust `script.js?v=424→425`, `document-manager.js?v=411→412`. Out of scope: backfilling existing docs, client portal, email templates, React `ClientDetailModal.tsx`. |
| 411 | [411-spouse-tab-show-all-templates.md](admin-ui/411-spouse-tab-show-all-templates.md) | COMPLETED — 2026-05-12 | Spouse tab on both add-doc surfaces (doc-manager + PA popover) was hiding all `scope=CLIENT` templates due to DL-352's scope filter. Office reported the spouse tab was missing options that exist on the client tab. Fix: drop the scope filter in both `_addDocTemplateMatchesPerson` (`frontend/assets/js/document-manager.js:1796`) and `_paTemplateMatchesPerson` (`frontend/admin/js/script.js:11263`) — both now always return `true`. Also simplify `buildDocMeta` person assignment at `document-manager.js:1854` so the active tab unconditionally tags `person` (was: tab OR `tpl.scope === 'SPOUSE'`). PA popover already used `st.person` from active tab — no change. Airtable `scope` field stays, just no longer consumed by the two add-doc UIs. P1 duplicate-path audit ✅ — both surfaces patched in one commit. DL-408 single-instance dedup invariant retained (documented out-of-scope). Cache-bust `document-manager.js?v=408→411`, `script.js?v=422→423`. Net `script.js` delta: −7 lines (ratchet auto-shrinks). |
| 410 | [410-rental-contract-nan-and-silent-refresh.md](admin-ui/410-rental-contract-nan-and-silent-refresh.md) | COMPLETED — 2026-05-11 | Two recurring complaints on rental-contract docs (T901/T902) in the AI-review screen: (a) the green "AI חושב שזה" pill renders `NaN.YYYY-NaN.YYYY` when `item.contract_period` is a truthy object but `startDate`/`endDate` is missing/empty (older T901 records reassigned before DL-397 shipped, or DL-359 partial-shape drift). Root cause: `appendContractPeriod` (`script.js:5973`) and the reviewed-card request-period block (`script.js:6346`) call `new Date(cp.startDate).getMonth()` without guarding missing dates → `NaN`. The outer `!item.contract_period` guard only catches `null`. (b) "+ [H:request-contract] MM.YYYY-MM.YYYY" doesn't refresh the UI — `requestMissingPeriod` (`script.js:7266`) only mutates the clicked button; the new follow-up doc appears only after page reload, violating CLAUDE.md P6. Fix: defensive guards at both NaN sites (render `__.__-__.____` placeholder when dates are missing, hide request-period buttons entirely on the reviewed card); silent refresh hook in `requestMissingPeriod` reusing `refreshItemDom` + `updateClientDocState` exactly as `saveContractPeriod`/`resubmitApprove` already do. Single source of truth — fixing `appendContractPeriod` propagates to all 12 callsites. Deleted unused `requestRemainingContract` shim (line 7314, zero callers) to fit under the monolith size ratchet. Cache-bust `script.js?v=419→420`. |
| 408 | [408-doc-manager-rental-multi-instance.md](admin-ui/408-doc-manager-rental-multi-instance.md) | COMPLETED — 2026-05-06 | Doc-manager add-doc dropdown silently dropped T901 ("[H:rented-out] – [H:income]") once it was already on the report (reported by Natan on a real-estate client). Two combined causes: (a) Airtable data drift — T901's `variables` field was empty, so the line-770 `userVars.length === 0 && existingTemplateIds.has(...)` filter at `frontend/assets/js/document-manager.js` treated it as single-instance and hid it; (b) the implicit "no variables ⇒ single instance" rule is wrong for rental contracts (one per property — multi-instance by nature). Fix: PATCH Airtable T901 templates row to set `variables = "rent_income_monthly"` (matching docs/airtable-schema.md and T902's `rent_expense_monthly`); add `MULTI_INSTANCE_TEMPLATES = new Set(['T901','T902'])` allowlist + bypass at the dedup line as defense-in-depth. PA cockpit needs no change — its `paDocIsDuplicate` (`script.js:11015-11050`) already keys on `issuer_key`, which now distinguishes properties once the variable is populated. Cache-bust `document-manager.js?v=384→408`. Future work: replace hardcoded set with a real `multi_instance` boolean column on the templates table. |
| 409 | [409-inbound-dedup-skip-pending-row.md](ai-review/409-inbound-dedup-skip-pending-row.md) | COMPLETED — 2026-05-12 | Inbound processor at `processor.ts:549-557` correctly detects file-hash duplicates and skips re-uploading to OneDrive, but still creates a fresh `pending_classification` row anyway ("but still create record if duplicate (with warning)" — comment lies, no warning is surfaced). Result: AI-review tab accumulates redundant rows when the same email is re-forwarded or replayed. CPA-XXX case in DL-407: 7 rows still pending from 2026-05-05 16:27 with file_hashes already matching today's Received docs. Open Phase A: silently skip vs. mark `is_duplicate=true` vs. soft-toast. Discovered during DL-407 investigation. |
| 407 | [407-fix-matched-doc-name-422-on-approve.md](ai-review/407-fix-matched-doc-name-422-on-approve.md) | COMPLETED — 2026-05-12 | `[review-classification]` was logging `422 UNKNOWN_FIELD_NAME: "matched_doc_name"` on every approve/reassign (~15 hits over 5 days) because the post-rename documents PATCH at `classifications.ts:2244-2248` copied a `pending_classifications`-only field onto the `documents` table. Single-block catch swallowed the 422 as "non-fatal" but also abandoned the legitimate `file_url` + `onedrive_item_id` post-rename update — so docs kept the pre-rename URL. Impact was cosmetic (OneDrive item-id access + SharePoint redirect kept links working — confirmed on CPA-YYY). Fix: drop `matched_doc_name` from docPatch, split the OneDrive `msGraph.patch` and the Airtable `updateRecord` into separate try blocks, funnel docPatch failures through `logError(category: 'DEPENDENCY')` so silent failures of this shape page in future. No backfill, no schema change, no type-allowlist (overengineered for a 1-field fix). Deferred: `airtable.updateRecord` type-narrowing per table; TABLES constant dedup. CPA-XXX's "docs went pending" complaint that prompted this DL traced to a separate inbound-dedup gap (`processor.ts:549-557` creates a fresh `pending_classification` even when `isDuplicate=true`) — tracked as DL-409. |
| 405 | [405-unify-context-menus.md](admin-ui/405-unify-context-menus.md) | COMPLETED — 2026-05-12 | Single source of truth for client-row actions: right-click, desktop kebab, and mobile kebab now all render from one shared item-list helper (`frontend/admin/js/modules/client-row-actions.js`). Ends the recurring drift where each new feature DL had to update both menus or one fell behind (DL-366 missed right-click, DL-404 missed kebab). 4 grouped sections (Send / View / Edit / Danger) with `<hr>` between non-empty groups. Mobile card long-press (≥500ms, cancels on scroll/touchmove) opens the same unified menu at finger position. ARIA `role="menu"`/`menuitem"` + arrow-key nav + Esc/Tab close. Net `script.js` delta: −80 lines (16217 → 16137); ratchet ceiling unchanged. Cache-bust `script.js?v=416 → 417`; new `<script src="js/modules/client-row-actions.js?v=1">`. Frontend-only — no API/schema changes. |
| 406 | [406-aging-colors-pending-notes-digest.md](admin-ui/406-aging-colors-pending-notes-digest.md) | COMPLETED — 2026-05-12 | Three layered nudges so the boss's expected "clear the queue" behavior actually happens. (1) **Aging colors** on three surfaces — shared `frontend/admin/js/modules/aging-colors.js` (window-attached IIFE) exposes `TIERS_MESSAGES` (Loose 48h SLA: <24h fresh / 1-2d / 2-5d / >5d stale) + `TIERS_REVIEW` (7d/14d, reserved for future use) and `ageTier(iso, tiers)`. Applied to: dashboard messages widget rows + groups (`.msg-row` / `.msg-group` get RTL `border-inline-start` stripes via `m.date` / `latest.date`); AI Review pending classifications (`.ai-review-card` gets a subtle bg tint via `item.received_at` — does NOT conflict with the existing `match-*` border-inline-start signal); PA queue priority badge (`.pa-card__priority` swaps DL-295's bespoke 3d/7d `--med`/`--high` modifiers for the unified `aging-day1`/`aging-aging`/`aging-stale` palette via `item.submitted_at`). Moshe-Review FIFO queue intentionally untouched. Cache-bust v=416→v=417. Net script.js: -3 lines (ratchet auto-bumps baseline DOWN). Color always paired with existing `formatRelativeTime` Hebrew text or numeric `N ימים` count for WCAG 1.4.1. (2) **Pending-notes digest section** — appended as a new sub-chain to WF07 (`0o6pXPeewCRxEEhd`, DL-204 daily digest), sourced from the SAME data the messages widget shows (Airtable `client_notes`, `hidden_from_dashboard !== true`, age <14d). Claude Haiku 4.5 with `output_config.format` (constrained-decoding JSON schema, replaces DL-204's regex-fence-strip) buckets into `urgent`/`regular`/`fyi` using HYBRID age+content rule (>=48h OR deadline/complaint/repetition signals). Few-shot prompt + explicit anti-hallucination skip-if-empty rule + warning that casual Hebrew `דחוף` doesn't auto-promote. Multi-message dedup (one entry per client, count=N). Verb-led 1-line `ask` field. Subject prepends urgent count when >0. Full prompt artifact: `docs/dl-406-pending-notes-prompt.md`. Phase D-1 (frontend) shipping first; Phase D-2 (n8n WF07) follows. |
| 404 | [404-merge-clients.md](admin-ui/404-merge-clients.md) | COMPLETED — 2026-05-12 | One-click merge of two clients into a single household when the office discovers a married couple was onboarded as separate records. Older `createdTime` always wins; loser is soft-archived with `merged_into` + `is_active=false`. All received documents physically moved into winner's OneDrive folder (collision rename ` (N)` before extension); Airtable child rows (`documents`, `pending_classifications`, `email_events`) re-pointed; loser's `clients.client_notes` free-form text appended to winner with a separator. Both source questionnaires preserved verbatim — print path renders both sequentially via `reports.merged_from_report_ids`. Email contract: To = winner.email, CC = winner.cc_email (was loser.email), extended from DL-183 questionnaire-only to ALL outbound types (reminders, batch status, approve-and-send) — `replyToMessage` deferred. Inbound identifier extended with Tier 1.5 cc_email match + `merged_into` follow-through (`match_method='merged_redirect'`). Merged display name pre-filled `<A> & <B>` and editable in the kebab confirm dialog; `winner.report.spouse_name` auto-set to loser.name when blank (warning on conflict). Stage = lower (earlier) wins; reminders cancelled and recomputed via DL-390 weekend-skip helper. Idempotent via KV lock + `loser.merged_into` check. Activity log: `client_merged` event with `client_id` only (no PII per DL-365). Schema additions (Airtable typecast on first PATCH): `clients.merged_into`, `clients.merged_at`, `reports.merged_from_report_ids`. Out of scope: cross-filing-type merge, un-merge, `replyToMessage` CC, deletion of empty loser OneDrive folder. |
| 403 | [403-bot-active-only-and-pluralization.md](infrastructure/403-bot-active-only-and-pluralization.md) | COMPLETED — 2026-05-12 | Two follow-ups from DL-402 deferred list. (1) `search_clients_by_name` should default to active clients only via `include_inactive: boolean = false` flag — current behavior surfaces inactive twins as noise. (2) `formatDocProgress` Hebrew pluralization: `"מסמך אחד מתוך N"` for count=1, `"X מתוך N מסמכים"` for count>1. ~30 min, single commit, single deploy. Verification: same `יש לקוח בשם ליעוז שור?` query should return 1 match (active only); a client with exactly 1 received doc should reply with singular `מסמך`. |
| 402 | [402-telegram-ops-bot.md](infrastructure/402-telegram-ops-bot.md) | COMPLETED — 2026-05-12 | Internal Telegram ops bot for Lioz/Natan/Moshe. Telegram (free, no BSP) chosen over WhatsApp (per-message pricing post-July-2025 + Meta general-AI ban Jan 2026). New `/webhook/telegram` route in `api/` Worker → constant-time `timingSafeEqual` on `X-Telegram-Bot-Api-Secret-Token` → 3-id allow-list → Claude Haiku 4.5 tool-use loop → existing Worker routes wrapped as tools → KV-backed 10-turn history. Clean-arch layered design with 4 narrow interfaces (`BotMessenger`, `ChatHistoryStore`, `ConfirmTokenStore`, `LlmClient`); `composition.ts` is the only file mixing vendor adapters + use-case layer. M1 ships 4 read-only tools (`get_dashboard_stats`, `get_client_by_report_id`, `get_client_documents`, `search_clients_by_name`); M2 adds confirm-flow + reminder/doc-status writes; M3 adds inbound-doc routing + notes + stage advance. AI-review = v2 (no existing endpoints). New env vars: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `ADMIN_TELEGRAM_IDS`. Zero touches to admin monolith (ratchet-safe). Code complete + 6 contract tests for `trimToTurnCap` green; live verification pending @BotFather setup, secret/var deploy, `setWebhook` script run. |
| 401 | [401-unidentified-doc-row-clickable.md](ai-review/401-unidentified-doc-row-clickable.md) | COMPLETED — 2026-05-12 | Unidentified-inbound doc rows in AI Review (`buildUnidentifiedDocsHtml`, DL-361) were intentionally read-only with `cursor: default` + `opacity: 0.85` and only an external-OneDrive ↗ icon. Now made clickable like classified rows (`script.js:4531`): single-line replacement at `script.js:5548` adds `data-id="${item.id}"` + `title` + `onclick="selectDocument('${item.id}')"`, drops inline cursor/opacity overrides. Reuses the existing `selectDocument` → `loadDocPreview` → `renderActionsPanel` flow (DL-334 perf instrumentation included for free). External ↗ icon preserved (existing `event.stopPropagation()` prevents conflict). Net line delta on script.js: 0 (ratchet-safe). Cache-bust `script.js?v=413→414`. Scope-adjacent observation logged for future DL: DL-112 file-hash dedup at `classifications.ts:172-180` hides older `unidentified-...` rows when newer identified twins exist with same hash — explains user's "3 forwarded → 1 visible" surprise. |
| 400 | [400-edit-client-modal-row-disappears-on-save.md](admin-ui/400-edit-client-modal-row-disappears-on-save.md) | COMPLETED — 2026-05-03 | Edit-client modal save wiped untouched fields in `clientsData` (delta payload from `ClientDetailModal.tsx` was unconditionally written to all 4 fields in `script.js:13919-13928`), causing `filterClients()` to drop the row until manual refresh. Fix: loop over `['name','email','cc_email','phone']` and only assign when `updated[k] !== undefined`. Cache-bust v=406→407. Compliant with P6 Silent-Refresh rule. |
| 398 | [398-stat-card-percentage.md](admin-ui/398-stat-card-percentage.md) | COMPLETED — 2026-05-03 | Admin dashboard stat cards: each of stages 1–8 now renders a small muted percentage next to the count (`% of counts.total` active clients, whole numbers). Total card unchanged. JS-only injection of `<span class="stat-pct">` inside `recalculateStats()` (`script.js:2128-2136` → 8-line loop) — no HTML markup change, single render path covers all 9 cards. New `.stat-pct` CSS rule (0.55em, gray-500, RTL-safe `margin-inline-start`). Cache-bust v=403→404 (script.js), v=384→385 (style.css). |
| 399 | [399-email-bounce-handling.md](admin-ui/399-email-bounce-handling.md) | COMPLETED — 2026-05-03 | Email bounce / NDR handling. Inbound `processor.ts` previously swallowed Outlook NDRs as auto-replies. New `bounce-detector.ts` extracts failed recipient + reason (DNS-not-found / mailbox-not-found / rejected) from Hebrew + EN NDR bodies; `bounce-handler.ts` clears `clients.email`, writes 4 new audit fields (`email_bounced`, `last_bounced_email`, `email_bounce_reason`, `email_bounce_at`), reverts Stage-2 reports to Stage-1, logs `email_bounce_handled` / `email_bounce_unmatched` / `stage_reverted_on_bounce`. Edit-client route lowers `email_bounced` flag when office sets a new non-empty email. Admin frontend (extracted to `modules/bounce-warning.js` due to ratchet): clickable ⚠ button next to stage badge (desktop+mobile) opening bounce-detail modal; pin bounced clients to top of table; Stage-1 stat-card "ממתינים לשליחה" shares DL-187 needs-attention bounce when any stage-1 client is bounced; green paper-plane row button + bulk-send disabled when `email` empty (toast-and-skip pattern); after edit-save with new email + Stage-1, prompts "לשלוח שאלון?". Schema: 4 new fields on `clients`, `Bounced` option added to `email_events.processing_status`. Cache-bust `script.js?v=403→404`. |
| 396 | [396-recent-messages-group-by-client.md](admin-ui/396-recent-messages-group-by-client.md) | COMPLETED — 2026-05-03 | Dashboard side-panel "הודעות אחרונות מלקוחות" buckets `_allMessages` by `client_name` (composite key with `client_id`) so multiple emails from one client collapse into ONE card. Header shows chevron + name + "N הודעות" counter + latest snippet preview; click expands to all messages with full per-message reply/folder/✓ buttons (DL-289 invariant preserved — no group-level ✓). Single-message groups render today's exact `.msg-row` (zero visual delta). Pagination semantic flips: `_messagesVisible` now counts *groups*, not messages — chatty clients no longer crowd out everyone. Expanded state survives re-render via `_expandedClients` Set. Frontend-only (`script.js`, `style.css`, `index.html` cache-bust v=400→401). |
| 397 | [397-manual-assign-contract-months-and-stale-template-id.md](ai-review/397-manual-assign-contract-months-and-stale-template-id.md) | COMPLETED — 2026-05-03 | Capture contract months (MM.YYYY) at manual assign to T901/T902 across 3 flows (reassign modal, chip menu, add-doc popover) using shared `renderContractMonthsInput` (DL-385 parser). Backend `reassign` extended to atomically persist `matched_template_id` and optional `contract_period` in one PATCH — fixes silent 400 from `request-remaining-contract` after manual reassign (Step 5 PATCH never wrote `matched_template_id`). |
| 395 | [395-pa-review-show-yes-answers.md](admin-ui/395-pa-review-show-yes-answers.md) | COMPLETED — 2026-05-02 | PA review tab `תשובות שאלון` section now mirrors the print sheet — every answer except `✗ לא` renders in one flat list (in original questionnaire order) instead of dropping all `✓ כן` answers. `buildPaPreviewBody` (`frontend/admin/js/script.js` ~10144-10182) replaces the 3-bucket partition with `visibleAnswers`/`noAnswers`; rows keep their original `answers_all` index for DL-302 cross-highlight stability. `pa-preview-subtitle "תשובות פתוחות (N)"` wrapper dropped. `togglePaShowNo` block + print button preserved. Cache-bust v=399→400. |
| 394 | [394-onedrive-copy-on-also-match.md](ai-review/394-onedrive-copy-on-also-match.md) | COMPLETED — 2026-05-02 | DL-391 follow-up: `also_match` now uploads a physical OneDrive copy per target (rename via resolveOneDriveFilename) instead of sharing one file. Rollback-on-failure. Cascade-revert becomes naturally per-card (unique item IDs). Worker-only change, no schema/frontend change. |
| 393 | [393-deemphasize-rejected-uploads-callout.md](email/393-deemphasize-rejected-uploads-callout.md) | COMPLETED — 2026-05-02 | De-emphasize "previously received / rejected uploads" amber callout in Type-B reminder emails. Soft amber (`#FFFBEB`), no border, smaller heading (13px/600), muted row text (`#78350F`), wrapper margin moved from bottom to top. Position swap: callout moves from above docs list to below (3 paths in n8n `FjisCdmWc4ef0qSV` Code node `Build Type B Email`: Hebrew-only, bilingual EN card, bilingual HE card; mirrored in Worker `buildDocSection` non-split + split modes in `api/src/lib/email-html.ts`). Removes alert-palette conflict with DL-127 questions card; reference info now reads as a footnote below the action items. |
| 392 | [392-document-leakage-audit.md](security/392-document-leakage-audit.md) | COMPLETED — audit, no implementation | Renumbered from DL-376 (number collision with OneDrive orphan rename). Read-only client-document-leakage audit across 8 surfaces (Workers API, doc URLs, client portal, email, AI Review, logs/R2/Airtable, n8n, admin browser). 17 findings P0-P2 incl. F-01 `/download-file` IDOR, F-02 raw PII to Anthropic in 4 LLM call sites, F-03 53-bit cyrb53 approval token (no expiry, non-timing-safe). Roadmap: 8 follow-up DLs, ordered B→C→A. |
| 390 | [390-reminder-next-date-skip-weekend.md](reminders/390-reminder-next-date-skip-weekend.md) | COMPLETED — 2026-05-12 | `reminder_next_date` calculator (`api/src/lib/reminders.ts`) shifts Fri/Sat → preceding Thursday so stored values match when DL-389 cron actually fires. Mirror in n8n WF[06] `Set Update Fields` post-send recompute. Temp admin endpoint `POST /webhook/admin-backfill-reminder-weekend-dates` (dryRun + filing_type) heals existing rows in both `annual_reports` + `capital_statements`. Backward-shift compresses minimum gap but stays above DL-168's 10-day floor. Holidays explicitly out of scope. |
| 391 | [391-chip-menu-assign-to-this-doc.md](ai-review/391-chip-menu-assign-to-this-doc.md) | COMPLETED — 2026-05-12 | DL-386 follow-up (renumbered from DL-390 — colliding with reminder-next-date-skip-weekend): chip menu in [required-docs] now offers "📎 שייך את התצוגה הפעילה למסמך זה" as the **first** option when (a) an AI cockpit card is preview-active, (b) the chip is `Required_Missing`, (c) chip's doc differs from active card's `matched_doc_record_id`. One-click — reuses `submitAIReassign(activeItemId, templateId, docRecordId)` (script.js:7773). `renderDocTag` gains `data-template-id`. Cache-bust v=394→395. |
| 386 | [386-add-required-doc-from-ai-review.md](ai-review/386-add-required-doc-from-ai-review.md) | COMPLETED — 2026-05-01 | "+" chip in AI review [H:required-docs] section opens the existing PA add-doc popover (DL-301/336) without leaving the tab. After add: silent refresh (DL-053). When exactly one card is `preview-active`, prompts to reassign that card's file to the new doc. Popover gains AI-mode fallback against `aiClassificationsData`. |
| 388 | [388-ai-review-action-flow-fixes.md](ai-review/388-ai-review-action-flow-fixes.md) | COMPLETED — 2026-05-01 | AI Review tab UX restoration: (1) `also_match` / "המסמך תואם למסמך נוסף" pre-validates `onedrive_item_id`+`file_url` before opening modal — disables the dead-end "Classification has no file to share" path; mirrors the guard in `moveClassificationClient`; server returns structured `code: 'no_file_to_share'` with Hebrew `message`; `formatAIResponseError` maps it. (2) `transitionCardToReviewed` now silently reconciles via `loadAIClassifications(silent=true)` after every approve/reject/reassign/also_match; auto-advance extended to cross-client fallback + mobile scroll-into-view; client-done prompt hands off to next client. Cache-bust v=391→392. |
| 389 | [389-block-weekend-automated-emails.md](email/389-block-weekend-automated-emails.md) | COMPLETED — 2026-05-12 | Block ALL client-facing automated emails on Friday/Saturday Israel time; release at Sunday 08:00. Extends DL-264/281: new helpers `isOffHoursOrWeekend()` + `getNextBusinessMorning0800Israel()` in `israel-time.ts`. Migrates 3 already-gated routes (approve-and-send, dashboard reply, send-batch-questions) and adds gate to 2 unprotected routes (send-questionnaires, request-pdf-password). n8n Reminder Scheduler `FjisCdmWc4ef0qSV` gets `Skip Fri/Sat (DL-389)` Code node. Codified in `email-design-rules.md` Section 13 — Sending Policy / Quiet Hours. Internal/office emails (feedback.ts, error logs, digest) intentionally exempt. UI reuses DL-281 Outlook source-of-truth modal — no frontend change. |
| 387 | [387-reassign-modal-single-click-custom.md](admin-ui/387-reassign-modal-single-click-custom.md) | COMPLETED — 2026-04-30 | Reassign modal: typing a custom doc name + clicking "שייך" now submits in one click (was 2 — Enter then Assign). Root cause: DL-350 commit-then-submit branch was dead code because the confirm button stayed disabled until chip commit. Fix: live `input` listener on `.ai-tpl-custom-input` in `_buildDocTemplatePicker` enables the button as soon as text is typed; existing `confirmAIReassign` early-return path handles the `_pending` target identically to the chip path. Cache-bust to v=389. |
| 384 | [384-password-reply-note-quote-stripping.md](ai-review/384-password-reply-note-quote-stripping.md) | COMPLETED — 2026-04-30 | Password-reply client_note shows the entire forwarded original PWD email instead of just the client's reply. Fix: persist the already-built `replyLines` (with `searchLines` fallback) to `password_reply_raw` + `client_notes[].raw_snippet` in `handlePasswordReplyEmail`. Also remove 2 dead `notesText = item.notes + item.client_notes` concats in admin script.js and stop dumping the JSON array onto the print sheet. DL-382 follow-up. |
| 385 | [385-partial-contract-swap-and-date-input.md](ai-review/385-partial-contract-swap-and-date-input.md) | COMPLETED — 2026-04-30 | T901↔T902 one-click swap button on AI-Review card (optimistic flip + undo toast); free-text MM.YYYY input above native month calendar in partial-contract date editor; 6-scenario QA Airtable view. |
| 382 | [382-batch-password-request.md](ai-review/382-batch-password-request.md) | COMPLETED — 2026-04-30 | Batch password request: one email for all encrypted PDFs of a client. Pre-checked nested checkbox list in preview modal. One shared token written to all selected records; inbound reply fans out to all matched rows. DL-380 follow-up. |
| 383 | [383-doc-manager-recover-waived.md](documents/383-doc-manager-recover-waived.md) | COMPLETED — 2026-04-30 | Doc-manager Waived↔Required reliability + UX: fix 500 on restore (doc-invariants skips file-field nulling for Waived sources, document_uid preserved); waive-wins guard in buildUpdateMap; real error body surfaced in admin toast; restore-checkbox → restore-btn; file-clear warning gated on non-Waived sources; immediate row refresh on save success. Also: 4 pre-existing TS errors fixed; hook path fixed with git rev-parse. |
| 381 | [381-backfill-stuck-review-timestamps.md](admin-ui/381-backfill-stuck-review-timestamps.md) | COMPLETED — 2026-05-12 | One-shot backfill: 2 clients had NULL `docs_completed_at` at Stage-5/Review, causing tab badge (49) vs stat card (51) gap. Patched both via Airtable REST PATCH using `createdTime` (2026-03-25). All Stage='Review' write paths audited — no active leaks. Dead `/admin-mark-complete` cleanup deferred. |
| 380 | [380-encrypted-ask-password-email.md](ai-review/380-encrypted-ask-password-email.md) | COMPLETED — 2026-05-12 | One-click "request password" bilingual email from AI Review kebab (DL-373/379 follow-up). New Worker endpoint `POST /webhook/request-pdf-password` (preview + send modes, idempotency guard). Inbound processor auto-detects `[#PWD-TOKEN]` in reply subject → writes `suggested_password` to `pending_classifications` → surfaces chip in unlock panel. Schema: 3 new fields + `PasswordReply` status option. |
| 379 | [379-encrypted-file-card-indicator.md](ai-review/379-encrypted-file-card-indicator.md) | COMPLETED — 2026-05-12 | Encrypted-PDF lock indicator on AI Review cards. Detects `ai_reason` containing "password protected" (Anthropic 400 error string already stored by inbound processor). Renders 🔒 "[H:hebrew]" badge + amber card border tint on affected cards. Frontend-only: 3-line JS change in `renderAICard`, 2 CSS rules. No Worker, no schema change. Cache-bust `script.js?v=379`, `style.css?v=379`. |
| 377 | [377-pre-commit-pii-enforcement.md](security/377-pre-commit-pii-enforcement.md) | COMPLETED — 2026-05-12 | Layered PII/secret defense after a session leaked client PII via `git commit --no-verify` to main. Harness `permissions.deny` blocks `--no-verify` and `--force` push variants; new CI workflow `pii-guard.yml` runs `agent-pii-guard.py --diff-range` + gitleaks on every push/PR; pre-commit gitleaks + ggshield wrapped in `timeout 30` so hung hooks fail fast instead of tempting bypass; PII guard regex extended with OneDrive itemIds, Airtable recIds (digit-required to skip camelCase words), and real-client email handles with allow-list for dev/office/example accounts. |
| 375 | [375-restore-download-button.md](ai-review/375-restore-download-button.md) | COMPLETED — 2026-04-29 | "Download" (Hebrew) button vanished from AI-review preview header for ALL files after DL-374. Root cause: DL-374 added `webUrl` to `$select` on `/me/drive/items/{id}` — `@microsoft.graph.downloadUrl` is an instance annotation that MS Graph silently omits under `$select`, so Worker returned `downloadUrl:""`. Fix: drop `$select` entirely (~1-2 KB extra payload) so both fields always come back. No frontend change — existing reveal at `script.js:3873-3876` self-heals. |
| 374 | [374-ai-review-open-tab-itemid.md](ai-review/374-ai-review-open-tab-itemid.md) | COMPLETED — 2026-04-29 | AI-review "open in new tab" anchor was using stale Airtable `file_url` (long Hebrew SharePoint path) → 404 after staff rename/move in OneDrive. Worker `/webhook/get-preview-url` now also `$select`s `webUrl` from MS Graph (zero extra calls — same item GET that fetches downloadUrl) and returns it in the JSON. `getDocPreviewUrl()` plumbs it through to `loadDocPreview` + `loadMobileDocPreview`; both anchors now hide upfront and reveal with `webUrl || item.file_url || ''` once the fetch resolves. DL-356 self-heal still fires on permanent 404 — toast + Airtable null + button stays hidden. Cache-bust `script.js?v=382→383`. Reuses `MSGraphClient`, `getDocPreviewUrl`, `handleFileGoneSelfHeal`. |
| 373 | [373-password-protected-pdf-unlock.md](ai-review/373-password-protected-pdf-unlock.md) | COMPLETED — 2026-05-12 | Password-protected PDF unlock from AI Review preview. Client-side PDF.js detects encrypted PDFs; server-side `@localonlytools/pdf-decrypt` decrypts; encrypted original archived to `/[H:hebrew]/encrypted-originals/` before replace. Rate-limited (5/min). New Worker route `POST /webhook/unlock-pdf`. |
| 372 | [372-pdf-sticky-note.md](ai-review/372-pdf-sticky-note.md) | COMPLETED — 2026-05-12 | PDF sticky-note annotation from AI Review card. pdf-lib /Annot Text placed at chosen corner (TL/TR/BL/BR) of page 1. Idempotent — strip+re-add on save. Note also mirrored to Airtable `documents.internal_pdf_note`. New Worker route `POST /webhook/add-pdf-note`. Shared infra: `MSGraphClient.putBinaryReplace`. |
| 371 | [371-edit-client-modal-ux-polish.md](admin-ui/371-edit-client-modal-ux-polish.md) | COMPLETED | Edit-client modal full redesign: new header `[H:hebrew]: {name}`, add name field (first input), inline SVG icons in labels, force `width:100%` on all input types (fixes phone shrink), cc_email placeholder, indigo Save button, proper 8px-grid spacing. Ships to both admin dashboard and doc-manager via single React bundle. 5/5 Vitest pass, tsc clean, build 189.98 kB. |
| 370 | [370-move-classification-edge-cases.md](ai-review/370-move-classification-edge-cases.md) | COMPLETED — 2026-04-28 | DL-369 follow-up: every successful `/webhook/move-classification-client` now lands the classification on the target client as `review_status: 'pending'` instead of `'reassigned'`, so unmatched moves and "fully clean" matched moves alike surface in AI Review under the target client. Target-doc-already-Received case no longer 409s — file is uploaded to target OneDrive anyway, the existing Received doc is left untouched, classification sits as pending with no `document` link. Source-clear skips if doc already Required_Missing (prevents 500 when two classifications share the same source slot). DL-248 source-clear guard, OneDrive old-item delete, `checkAutoAdvanceToReview`, cache invalidation, and security log all preserved. |
| 369 | [369-ai-review-move-document-to-client.md](ai-review/369-ai-review-move-document-to-client.md) | COMPLETED — 2026-05-12 | AI Review card overflow menu now includes `[H:hebrew]...` for every card state. Opens a searchable active-client picker excluding the source client, confirms with a custom dialog, and calls new Worker endpoint `POST /webhook/move-classification-client`. Backend moves only the current classification/file: resolves target same-filing-type active report, reclassifies against target missing docs, uploads to target OneDrive folder with DL-355 filename rules, clears the source document via DL-356 invariant only when it still references the moved file, patches target doc/classification, deletes old OneDrive item after target upload, and returns structured errors such as `ambiguous_target_report`. |
| 368 | [368-cf-pages-git-integration-broken.md](infrastructure/368-cf-pages-git-integration-broken.md) | COMPLETED — 2026-05-12 | CF Pages `annual-reports-client-portal` stopped firing builds from `main` pushes since 2026-04-27 evening. Root cause: stored `source.config.repo_id=1136319991` no longer matches GitHub's actual repo id `1222817442` — the GitHub repo was deleted and recreated at some point, orphaning CF's binding. GitHub PushEvents on main delivered fine; CF just ignored them. All 10 prod deploys today were `ad_hoc` from manual `wrangler pages deploy` with `commit_dirty=true`. Fix: (1) catch-up deploy of clean main HEAD `21c1488` shipped via wrangler from a temporary worktree; (2) USER must reconnect Git integration in CF Dash → Pages → Settings → Git integration (no public API for repo rebinding); (3) verification via empty trigger commit + checking next deploy has `trigger.type=github:push` and CF `repo_id=1222817442`. Suggests follow-up: nightly drift check comparing CF `repo_id` vs GitHub `id`. |
| 367 | [367-gmail-drive-smart-links.md](email/367-gmail-drive-smart-links.md) | COMPLETED — 2026-05-12 | Inbound pipeline now fetches Gmail "Insert from Drive" smart-link attachments (live case: CPA-XXX, 4 PDFs lost 2026-04-28). Gmail embeds them as inline `gmail_drive_chip` HTML cards with `hasAttachments=false`, so `fetchAttachments` returned 0 and the email completed silently. New `parseDriveLinks(bodyHtml)` extracts `{fileId, filename}` from chip divs (matched by `class*=gmail_drive_chip` + `id="<fileId>"`, with `title="..."` on inner `<div>`) and bare `drive.google.com/file/d/...` URLs; `fetchDriveAttachment(link, maxBytes=25MB)` calls `https://drive.usercontent.google.com/download?id={id}&export=download&authuser=0&confirm=t` (post-May-2024 endpoint), validates Content-Type (rejects HTML "you need access" page), streams with byte cap, computes sha256, synthesizes the same `AttachmentInfo` shape as Graph attachments — pipeline downstream is unchanged. New `stripDriveChipsFromHtml` removes chip divs in `extractMetadata` before HTML→text so chip filenames don't pollute the LLM-summarized note. `ghostAttachments` guard extended to also fire when Drive links found but all fetches failed. Per-file failure → email_event `NeedsHuman` with Drive URLs preserved in `error_message`. Backfill confirmed: 4 pending_classifications with proper Hebrew names + 3 T501 matches at conf 0.95. Files: `attachment-utils.ts`, `processor.ts`. |
| 366 | [366-kebab-add-cc-email-and-resend.md](admin-ui/366-kebab-add-cc-email-and-resend.md) | COMPLETED — 2026-05-12 | Two new dashboard kebab actions: (1) "[H:hebrew]/[H:hebrew]" opens `ClientDetailModal` auto-focused on `cc_email` (new `focusField` prop threaded through bridge → island → component); on save when stage=`Send_Questionnaire`, prompts `showConfirmDialog` to re-send via existing `sendSingle(rid)`; off stage 1, toast only. (2) "[H:hebrew]" (stages 1–3) calls new Worker route `POST /webhook/admin-questionnaire-link` which mints a fresh signed URL via new helper `api/src/lib/questionnaire-url.ts` (extracted from `send-questionnaires.ts:81`); frontend writes URL to clipboard. Dashboard API (`dashboard.ts`) now returns `cc_email` per row via parallel clients-table fetch + Map join. Cache-bust `script.js?v=369→370` (rebased over DL-364). Out of scope: extending CC to non-questionnaire emails (deferred). |
| 365 | [365-activity-logger.md](infrastructure/365-activity-logger.md) | COMPLETED — 2026-05-12 | Replace Airtable security_logs with Cloudflare-native activity logger (Workers Logs + R2). Phase 1 (Foundation) shipped + verified live. PII strategy: client_id-only logs + viewer-side Airtable join. Phases 2-4 queued. |
| 364 | [364-review-tab-v-button-and-count-mismatch.md](admin-ui/364-review-tab-v-button-and-count-mismatch.md) | COMPLETED — 2026-05-12 | Fix two bugs on the "[H:hebrew]" (Stage 5/Review) admin screen: (1) the "v" (circle-check) row button currently calls `/admin-mark-complete` and jumps stage 5 → 8, silently skipping `Moshe_Review` and `Before_Signing` — rebound to call `/admin-change-stage` with `target_stage='Moshe_Review'` so it advances exactly one stage as expected, with new Hebrew wording (`[H:hebrew]?` / `[H:hebrew]`). (2) Stat card showed 49 vs tab badge 47 — root cause: `recalculateStats()` counts every Stage-5 client while `dashboard.ts` `review_queue` filter requires `docs_completed_at` set; clients moved to Review manually via the dropdown bypass the natural completion flow that sets the timestamp. Fix in backend `/admin-change-stage`: when `target_stage === 'Review'` AND `docs_completed_at` is empty, set it to `now()`. Both surfaces now agree on subsequent loads. No new endpoints; reuses existing `/admin-change-stage` (DL-155 reminder cleanup applies automatically). Frontend cache-bust on `script.js`. Out of scope: deleting the now-unused `/admin-mark-complete` endpoint, one-time backfill of 2 currently-stuck clients (resolves naturally as they move out and back in), extending pattern to other queue tabs. |
| 362 | [362-doc-manager-chat-bubbles.md](admin-ui/362-doc-manager-chat-bubbles.md) | COMPLETED | Redesign doc-manager client-notes timeline as chat-bubble conversation view. Office bubbles on RIGHT (me/admin), client bubbles on LEFT (Israeli WhatsApp RTL convention). Letter avatars on first-of-run only; date dividers between Outlook threads; oldest-first message order within a thread; hover-revealed edit/delete; `batch_questions_sent` rendered as centered system notice. DL-360 bucket logic preserved; `toggleCnThread` deleted. |
| 361 | [361-unidentified-inbound-routing.md](ai-review/361-unidentified-inbound-routing.md) | COMPLETED — 2026-05-12 | Surfaces unidentified inbound emails in AI Review as virtual accordions (one per email) so the office can assign-to-client (re-classifies + moves OneDrive files) or discard. Also adds Tier 2.5 forwarded-sender-NAME match in `client-identifier.ts` (token-overlap on display name from `From: <Name> <email>` header) and loosens Tier 4 AI matching from strict `client_name === aiClientName` to token-overlap. New endpoint `POST /webhook/assign-unidentified` (assign|discard). Replaces silent-bucket early-return at `processor.ts:777-797` — unidentified rows now create pending_classifications with `client_id=''` so they appear in AI Review. Frontend: `renderAICards` groups by `__unidentified__:<email_event_id>` when client_id empty; new `buildUnidentifiedListRowHtml` + `buildUnidentifiedDocsHtml` + `showAssignUnidentifiedModal` (searchable client picker over `clientsData`). Schema additions applied via Airtable typecast: `email_events.match_method` += `forwarded_name` + `manual_assignment`; `email_events.processing_status` += `Discarded`; `pending_classifications.notification_status` += `discarded`. Cache-bust `script.js?v=363→364`. Live incident (CPA-XXX, 2 events 2026-04-27 from `forwarded@example.com`) backfillable via the new endpoint. |
| 360 | [360-doc-manager-thread-grouping.md](admin-ui/360-doc-manager-thread-grouping.md) | COMPLETED — 2026-05-12 | Group doc-manager client-notes timeline by Outlook `conversationId`. Persist `conversationId` in MS Graph `$select` + note entry (`processor.ts`); office replies inherit parent's `conversation_id` (`dashboard.ts`). Backfill endpoint `/webhook/backfill-conversation-ids` fetches historic conversation IDs from Graph and patches existing notes. Frontend `renderClientNotes` buckets email notes by `conversation_id` into threads; renders one card per thread — latest message visible, older messages collapsed behind a "▸ [H:hebrew] N [H:hebrew]" toggle. Single-message threads render as today (no toggle). Standalone notes (no `conversation_id`) unaffected. |
| 359 | [359-edit-full-year-contract-dates.md](ai-review/359-edit-full-year-contract-dates.md) | COMPLETED | AI Review T901/T902 full-year contract badge made clickable to override LLM verdict. Click swaps the green `📅 [H:hebrew] ✓` badge for the partial-mode editor (same UI as DL-270) pre-filled with AI-detected dates; save re-evaluates `coversFullYear` server-side via existing `update-contract-period` endpoint and the banner reverts to whichever state matches new dates. Frontend-only: extract `renderFullYearBadge(rid, year)` + `renderContractPeriodBanner(rid, cp, year)` helpers in `script.js`, swap inline rendering at AI-review card (line ~4804) to call them, add `expandFullYearBadgeToEdit(rid, badgeEl)` (replaces outerHTML), refactor `saveContractPeriod` post-save UI swap to use both helpers bidirectionally — also fixes the pre-existing `.period-label` no-op bug on partial→full transitions. Pending Approval queue (5739) + mobile banner (781) intentionally out of scope. Cache-bust: `script.js?v=363→364`. |
| 358 | [358-remove-greeting-in-comment-email.md](email/358-remove-greeting-in-comment-email.md) | COMPLETED | Remove the `[H:hebrew] ${clientName},` greeting row from `buildCommentEmailHtml` (`api/src/lib/email-html.ts:702`). Comment email now opens directly with the bookkeeper's typed text under the blue header bar — chat-like, less letter-like. SSOT propagates to the live preview (DL-289 `/admin-comment-preview`) without duplicate edits. Other Hebrew templates (questionnaire, batch-status, generic) intentionally untouched. `clientName` retained in `CommentEmailParams` interface for caller compatibility but dropped from destructure with a `DL-358` marker. DL-289 testing checklist line 111 backfilled. Closing `[H:hebrew], [H:hebrew]"[H:hebrew] Client Name` line preserved per Phase A. |
| 356 | [356-preview-url-stale-itemid-self-heal.md](infrastructure/356-preview-url-stale-itemid-self-heal.md) | COMPLETED — 2026-05-12 | Self-heal stale `onedrive_item_id` references on `/webhook/get-preview-url` 404. Centralize the DL-205 invariant at the data-write layer (new `api/src/lib/doc-invariants.ts`) and call from `edit-documents.ts` + `classifications.ts` (reject/reassign/revert_cascade). On MS Graph 404+itemNotFound, `preview.ts` PATCHes the row's file fields to null (when `recordId` provided) and returns structured `{code:'FILE_GONE'}`. Frontend toasts in Hebrew + locally re-renders the row without preview button. New admin-only `GET /webhook/audit-stale-itemids?dryRun=1` mirrors backfill.ts pattern to find/clean residual records. Cross-report duplicate (DL-230) accepted as-is — fix is record-scoped. |
| 355 | [355-onedrive-rename-always-short-name.md](documents/355-onedrive-rename-always-short-name.md) | COMPLETED — 2026-05-12 | OneDrive filenames inconsistent across paths: `[H:hebrew] 106 – [H:hebrew] 106.pdf` (no-bold fallback duplicating template name into `{issuer}`), literal `{year}` in T501 names, admin upload + inbound + split bypassing `buildShortName`. Fix: (1) add echo-detection in `buildShortName` Step 3 — when no `<b>...</b>` AND input equals `HE_TITLE`/`name_he`/pattern-stripped, suppress fallback so `{issuer}` is stripped cleanly; (2) new `resolveOneDriveFilename({templateId, issuerName, attachmentName, templateMap, suffix?})` single source of truth for ALL OneDrive write paths; (3) wire the 4 bypass paths (admin upload, inbound `buildExpectedFilename`, split-classify segment rename, approve no-longer-gated-by-match-quality) through it; (4) Documents PATCH post-rename now propagates `issuer_name` + `matched_doc_name` + `expected_filename` (fix for admin views showing blank columns after approve). Audit script `scripts/audit-short-name-he.mjs` (read-only) flags `{year}`/parentheticals/overlong patterns in Airtable templates. No OneDrive backfill (per user). T901/T902 rental-period suffix preserved via `resolveOneDriveFilename({suffix})`. |
| 354 | [354-approve-and-send-idempotency.md](email/354-approve-and-send-idempotency.md) | COMPLETED — 2026-05-12 | Duplicate `[H:hebrew]` email observed (client@example.com, 16:16 twice). Root cause: `api/src/routes/approve-and-send.ts` has no idempotency guard between `graph.sendMail` (Step 5) and the Airtable write (Step 6) — double-click, two-tab, or Worker retry can fire two sends. Proposed: re-read `docs_first_sent_at` and set it to `now` *before* `sendMail`; treat second caller as no-op. Optional KV lock `lock:approve:<reportId>` for race-tight protection. |
| 353 | [353-reject-reason-optional.md](ai-review/353-reject-reason-optional.md) | COMPLETED | AI-Review inline reject pane: drop the `disabled` gate on the confirm button so admin can reject in one click without picking a reason. Dropdown placeholder relabeled `[H:hebrew] ([H:hebrew])`. Display-layer fallback `[H:hebrew]"[H:hebrew]` substituted in admin reviewed-card (`script.js` L4659, L5792) when no reason is stored; backend email-callout fallback (`api/src/lib/email-html.ts:250`) flipped from `[H:hebrew]`/`Other` → `[H:hebrew]"[H:hebrew]`/`Rejected by office`. Batch/persistent-review modal (L6550) intentionally left untouched. No API contract or schema change. Cache-bust `script.js?v=352→353`. |
| 352 | [352-add-doc-owner-tabs.md](admin-ui/352-add-doc-owner-tabs.md) | COMPLETED — 2026-05-12 | Replace sticky `#spouseDocCheckbox` (DL-162) in doc-manager with a `[👤 {client_name}] [👥 {spouse_name}]` segmented control above the combobox. Defaults to client every page load (no sticky state — root-cause fix for the "wrong owner" bug). Hidden when no spouse. Combobox now filters by template `scope` (CLIENT/SPOUSE/PERSON/GLOBAL_SINGLE/empty) via predicate ported from PA popover (`script.js:9500`). Search input preserved across tab switches. PA popover already had this pattern (DL-301) — both surfaces now uniform. Files: `frontend/document-manager.html` (line 270 swap), `frontend/assets/js/document-manager.js` (state `_addDocActivePerson`, helpers `setAddDocPerson`/`renderAddDocPersonTabs`/`_addDocTemplateMatchesPerson`, `isSpouseDocMode` rewrite, scope filter in `renderAddDocDropdown`), `frontend/assets/css/document-manager.css` (mirrors `pa-add-doc-person-*`). |
| 351 | [351-doc-tag-menu-edit-delete.md](ai-review/351-doc-tag-menu-edit-delete.md) | COMPLETED — 2026-05-12 | Add Edit (inline rename) + Delete (waive with confirmation) actions to the AI Review doc-tag menu (`openDocTagMenu` script.js:7976), below the existing 3 status options separated by a divider. Reuses `name_updates` extension on `/edit-documents` for rename, and existing `updateDocStatusInline(..., 'Waived')` for delete (no new API). Inline rename UX: tag inner becomes `<input class="ai-doc-tag-rename-input">`, Enter commits, Esc/blur cancels, empty/unchanged = no-op. Delete uses `showConfirmDialog` then delegates to existing waive flow. Optimistic update + undo toast for both. New `.ai-doc-tag-menu-divider`, `.ai-doc-tag-menu-item--danger`, `.ai-doc-tag-rename-input` CSS. Cache-bust: `script.js?v=351→352`. PA queue (`openPaDocTagMenu`) untouched — out of scope. |
| 350 | [350-reassign-locked-button-and-404.md](ai-review/350-reassign-locked-button-and-404.md) | COMPLETED | AI Review reassign bundle (live 2026-04-26). Started as a 1-line scope fix (combobox `onSelect` button-lookup missing `.ai-actions-panel` ancestor after DL-334/339), grew to a flow rework. Backend Path 3 fallback creates a DOCUMENTS row when picker-added templates don't yet exist on the report; rejects empty derived names. Picker derives userVars from `name_he`/`short_name_he` placeholders too, not just `tpl.variables`. Frontend forwards `newDocName` for any `template_id`. Modal hides combobox while picker is open; combobox reopens on second click. Doc-tag refresh anchored on `.ai-missing-docs-body` previous sibling. In-place refresh for picker-created docs via `data.doc_id`. Missing-docs body capped at 240px scroll. "[H:assign]"/"[H:hebrew]" auto-commit typed custom name. Inline unmatched + issuer-mismatch-fallback states drop the inline combobox → "[H:assign] [H:hebrew]" button opens the modal. Cache: `script.js?v=339→362`, `style.css?v=318→319`. Worker `8da9e5c9 → 10157460`. |
| 349 | [349-doc-tags-header-refresh.md](ai-review/349-doc-tags-header-refresh.md) | COMPLETED | Fix DL-330 regression: AI Review pane-2 doc-tag header (`.ai-missing-docs-body`) was a silent no-op on desktop because `refreshClientDocTags` only knew the legacy `.ai-accordion[data-client]` selector. Made `refreshClientDocTags` layout-aware. Added `refreshClientRowStats(clientName)` and shared helper `buildDocCategoryTagsHtml`. Cache-bust: `script.js?v=339→349`. |
| 348 | [348-banner-compact.md](ai-review/348-banner-compact.md) | COMPLETED | Compact the AI Review completion banner. Height targets: both flows ≤95px (~80), one flow ≤75px (~60), neither ≤50px (~40). Single-line header (icon + bold title + ` · ` + 11px stats); inline `.ai-review-flow-row` replacing `.ai-review-flow-card` (no nested white card); conditional primary placement — inlined at end of `.ai-review-done-content` when no flow rows render (banner collapses to one ~40px row), else `.ai-review-done-footer` below the flow stack. `flex-wrap: wrap` + `min-width: 0` + `text-overflow: clip` chosen over ellipsis (per user: "data > 1-line guarantee") for pathological 4×2-digit stats case. Outer padding tightened to `10px 12px`. Primary: `padding: 6px 16px; font-size: 12px;` (no min-width). Send button: `padding: 3px 10px; font-size: 11px;`. No class renames — CSS rules replaced in place. Hierarchy from DL-347 preserved (single solid green only). DL-345 wording flip and DL-346 conditional logic preserved. Cache-bust: `style.css?v=316→317`, `script.js?v=338→339`. |
| 347 | [347-banner-hierarchy-invert.md](ai-review/347-banner-hierarchy-invert.md) | COMPLETED | Invert visual hierarchy of the AI Review completion banner. Filled/Outlined/Text triad: single solid green primary `[H:hebrew]` always rendered at the bottom (the only solid green in the banner); both card "send" buttons (`[H:hebrew]`, `[H:hebrew]`) demoted to outlined ghost (white bg, `--success-700` text, 1px `--success-500` border); `[H:hebrew]` and `[H:hebrew]` demoted to text-links. Header inline dismiss removed. Reasoning: irreversible "send email" action was the loudest control while the reversible "finish review" was a small ghost — wrong consequence emphasis. Same flags + same handlers as DL-346; DL-345 confirm-dialog wording flip preserved. Scoped class names (`.ai-review-flow-card__send-btn`, `.ai-review-flow-card__link`, `.ai-review-done-primary`) — no global `.btn` changes. Cache-bust: `style.css?v=315→316`, `script.js?v=337→338`. |
| 346 | [346-completion-banner-two-flows.md](ai-review/346-completion-banner-two-flows.md) | COMPLETED | Restructure the AI Review "all reviewed" banner from one mixed action area into two self-contained white sub-section cards inside the green outer banner. Sub-section A (questions) renders iff `hasPendingQuestions`; primary `[H:hebrew]` + ghost `[H:hebrew]` + ghost `[H:hebrew]`. Sub-section B (missing docs, DL-345 logic preserved) renders iff `docsMissing > 0 && reportId`; primary `[H:hebrew] [H:missing-docs]` + ghost `[H:hebrew]`. Single `[H:hebrew]` dismiss only when neither flow applies. DL-345 amber `<details>` chip + bottom action row deleted (info now lives in card B's context line). Plural-aware Hebrew (`1 [H:hebrew]` vs `N [H:hebrew]`, `[H:hebrew] 1 [H:hebrew]` vs `[H:hebrew] N [H:hebrew]`). All onclick handlers unchanged. Mobile out of scope. Cache-bust: `style.css?v=314→315`, `script.js?v=335→336`. |
| 345 | [345-aireview-done-prompt-doc-status.md](ai-review/345-aireview-done-prompt-doc-status.md) | SUPERSEDED BY DL-346 (presentation only — logic preserved) | AI Review "all reviewed" prompt initially gained an inline X/Y chip + send-missing-docs action row reusing DL-308 `previewApproveEmail` + `ENDPOINTS.APPROVE_AND_SEND` (`approveAndSendFromAIReview`). DL-346 then re-presented the same logic as a dedicated sub-section card; the chip + bottom action row markup was removed but the underlying handler + endpoint remain in active use. |
| 344 | [344-reject-clears-unrelated-approval.md](ai-review/344-reject-clears-unrelated-approval.md) | COMPLETED | Bug fix (CPA-XXX): rejecting an AI-classification card was wiping the file an earlier approve had put on the same source doc when WF05 pre-linked multiple classifications to one DOCUMENTS row. Mirror of DL-248's reassign guard, applied to reject branch (`api/src/routes/classifications.ts:1495-1530`): skip the source-doc PATCH when `srcDoc.onedrive_item_id !== cls.onedrive_item_id`. Step 5 (cls→`rejected`) and `rejected_uploads_log` append still run unconditionally. Includes manual repair plan for `recpvynrLkdhO1Aiz` (T002). |
| 343 | [343-burst-stagger-and-update-hardening.md](reminders/343-burst-stagger-and-update-hardening.md) | COMPLETED — 2026-05-12 | Follow-up to DL-342 audit. User opted single-day burst with intra-run stagger. Inspection of WF[06] revealed (a) Send Email already paces at 2.5s via built-in `options.batching` (no change needed) and (b) the real silent-failure risk was post-send Airtable writes — `Update Reminder Fields` and `Update Skipped Airtable` had no retry/onError, so any single failure would block all 422 `last_reminder_sent_at` writes → next cron resends entire cohort. Two MCP `updateNode` ops applied: `retryOnFail:true, maxTries:3, waitBetweenTries:1500, onError:'continueRegularOutput'` on both Airtable nodes. Cron, batching, active state, `availableInMCP` all preserved. DL-154 24h-window bug still queued, not hot-fixed. |
| 342 | [342-reminder-burst-readiness.md](reminders/342-reminder-burst-readiness.md) | COMPLETED — 2026-05-12 | Audit-only readiness check for the 422-reminder burst this week. Cron-only delivery (WF[06] @08:00 IL); peak ~85–150/day vs. 2,000/day Workspace cap → 13× headroom. Bottleneck table: HIGH = single n8n execution silent partial-failure; MEDIUM = DL-154 24h-window idempotency still latent + node order (write `last_reminder_sent_at` before/with Gmail send) + observability gap. Verdict: **GO** with manual spot-check of WF[06] pre-Monday + live monitor of first two 08:00 runs (n8n executions + `wrangler tail`). DL-154 left as queued hot-fix candidate, not deployed pre-burst. No code changes. |
| 341 | [341-preview-zoom-and-completion-flow.md](ai-review/341-preview-zoom-and-completion-flow.md) | COMPLETED | AI Review cockpit triple: (1) preview zoom default 75% via Graph POST body `{viewer:'onedrive', zoom:0.75}` + `&nb=true` banner hide on returned `getUrl`; (2) fix desktop last-doc done-prompt regression from DL-334 — `showClientReviewDonePrompt` now layout-splits into mobile (accordion) + desktop (prepended into `.ai-review-docs` above `.ai-doc-list`), shared `_buildClientReviewDonePromptEl` builder; (3) Gmail-style auto-advance in `transitionCardToReviewed` — when `pendingLeft > 0` and desktop, pick oldest remaining pending via `pendingItems.sort(compareDocRows)[0]` → `selectDocument(nextId)`. Mobile auto-advance skipped. Files: `api/src/routes/preview.ts`, `frontend/admin/js/script.js`, `frontend/admin/index.html`. Cache-bust: `script.js?v=327→328`. |
| 340 | [340-reviewed-indicator-on-preview.md](ai-review/340-reviewed-indicator-on-preview.md) | COMPLETED | Layered reviewed-state signal for AI Review. Preview pane: `✓/⚠/↻` badge in header + 3px colored `border-inline-start` on `.ai-preview-frame` + rubber-stamp watermark (rotated -8°, 3px border + inner ring) in top-start corner of iframe area. Pane-2 rows: reviewed rows dim to `--gray-500` + state-colored strikethrough on filename, category swaps for compact chip ([H:hebrew]/[H:hebrew]/[H:hebrew]), rows sort by state group (pending → on_hold → reviewed) with on-transition relocation in `refreshItemDom`. Single `applyPreviewReviewState()` + `compareDocRows` drive all surfaces from one `review_status`. No new tokens. Cache: style.css v=300→313, script.js v=314→327. |
| 339 | [339-move-actions-to-pane2.md](ai-review/339-move-actions-to-pane2.md) | COMPLETED — 2026-05-12 | Move AI Review actions panel from pane 3 (below preview) into pane 2 (below doc list) — pane 3 becomes 100% preview, pane 2 becomes flex column with 60/40 list/panel split driven by `.has-selection` on `.ai-review-docs`. `flex-basis` transitions 180ms; `selectDocument` first-click scrolls active row into view 200ms post-transition (DL-278 pattern). Bundles 3 fixes: Fix A (bidi — replace `unicode-bidi: isolate` + `dir="auto"` with `unicode-bidi: plaintext; text-align: start` so Latin-starting filenames align identically to Hebrew); Fix B (truncation — new `truncateKeepExtension(name, 45)` replaces `truncateMiddle`, preserves `.pdf` extension); Fix C (missing-docs — replace legacy `.ai-missing-docs-body { max-height: 0 }` accordion with dual-selector display toggle for mobile + desktop). Cache-bust: `style.css?v=305`, `script.js?v=321` |
| 338 | [338-ai-review-messages-hover-reply.md](ai-review/338-ai-review-messages-hover-reply.md) | COMPLETED — 2026-05-12 | AI Review tab client messages timeline: hover reveals reply button + 2-line clamp unclamped on hover. `showReplyInput` patched with 1-line OR-selector to also match `.ai-cn-entry[data-note-id]`. `renderEntry` adds data attrs + `.ai-cn-action-btn`. CSS: `.ai-cn-entry` gets `flex-wrap`, hover bg; `.ai-cn-summary` 2-line clamp; new `.ai-cn-action-btn` opacity-0→1 on hover. script.js v=306→307, style.css v=296→297 |
| 337 | [337-raw-text-instead-of-ai-summary.md](ai-review/337-raw-text-instead-of-ai-summary.md) | COMPLETED — 2026-05-12 | AI Review tab client-notes timeline now shows raw client email text (`raw_snippet`) instead of AI summary; falls back to `summary` for legacy notes / manual notes. Matches Dashboard Recent Messages + Pending-Approval modal fallback pattern (`script.js:1083`, `:7521`). Doc-Manager exempt — still shows "[H:hebrew] AI:" labeled summary. Backend + summarizer unchanged. script.js v=305→306 |
| 334 | [334-cockpit-middle-and-actions.md](ai-review/334-cockpit-middle-and-actions.md) | COMPLETED — 2026-05-12 | AI Review cockpit phase 1 (v2 plan) — pane 2 thin rows (28-30px, state-stripe including on_hold amber) + pane 3 vertical split (preview top / state-aware actions panel bottom). Filename renders ONCE in panel header, never in row. Flat-minimal (0.5px borders, sentence case, 400/500 weights, no new tokens). Full on_hold integration: row category `⏳ [H:hebrew]`, panel lozenge, body shows question + AI guess, single `[H:hebrew]` button → `startReReview`. Coordinates with DL-335 (no changes to `dismissAndSendQuestions` / `dismissClientReview` / `renderReviewedCard`; DL-334 only consumes their output). Bundles DL-053 silent-refresh merge-by-id. Mobile <768px untouched via `isAIReviewMobileLayout` guards. Supersedes the reverted v1 attempt (commit `1ef907f`, reverted via `f643a79`). Status DRAFT — awaiting implementation approval |
| 333 | [333-batch-questions-off-hours-queue.md](email/333-batch-questions-off-hours-queue.md) | COMPLETED — 2026-05-12 | DL-328 "[H:hebrew]" batch-questions email now defers to next 08:00 Israel when sent off-hours (20:00-08:00). Mirrors DL-264/273 PidTagDeferredSendTime pattern; `client_notes` `batch_questions_sent` entry stamped with `graph_message_id` + `queued:true` so DL-281 queue modal surfaces it as a third row type (`[H:hebrew]`). Toast swaps to info-tone "[H:hebrew] — [H:hebrew]08:00" when queued. Preview path unchanged. `QueuedRow` type extended to `'doc_request' \| 'reply' \| 'batch_questions'`. script.js v=297→v=298. Zero schema change |
| 336 | [336-template-picker-ui.md](ai-review/336-template-picker-ui.md) | COMPLETED | Template picker UI in also-match + reassign modals: replaces `createDocCombobox` free-text path with search + categorized template list + variable wizard + chip feedback. New `_buildDocTemplatePicker(container, item, opts)` reuses `ensurePaTemplatesLoaded` + `pa-add-doc-*` CSS. `createDocCombobox` gets backwards-compatible `onExpand` hook. script.js v=302→303, style.css v=295→296 |
| 335 | [335-ai-review-on-hold-docs.md](ai-review/335-ai-review-on-hold-docs.md) | COMPLETED — 2026-05-12 | Hold state for AI Review docs awaiting client reply: `review_status='on_hold'` keeps rows in queue after questions sent; amber "[H:hebrew]" card + "[H:hebrew]" resolve button; `client_notes` entry extended with `id`/`summary`/`source` to render in per-client timeline ([H:hebrew]) as outbound card. `dismissClientReview` keepOnHold filter. script.js v=298→299 |
| 332 | [332-ai-review-pane1-density.md](ai-review/332-ai-review-pane1-density.md) | COMPLETED — 2026-05-12 | AI Review pane 1 density redesign (Phase 3 of cockpit plan, standalone). Drop `user` icon + pill badges from client rows; inline amber `.ai-client-pending-num` (`--warning-500`, `font-weight: 500`) in place of the "N [pending-label]" green pill; zero-pending clients render nothing in the pending slot. Row chrome trimmed (`padding: --sp-2/--sp-3`, `min-height: 0`) so 12+ clients fit per 1080p viewport (vs. ~6 today). Folder-open icon stays on start side (visual-right in RTL). Selected-row palette preserved; subtitle demoted to `--gray-600` on active, pending number demoted to `--gray-700`. Mobile unchanged. Cache-bust v=293→294 / v=297→298. All DL-330 / DL-306 / DL-278 / DL-053 hooks intact. Impl via `/subagent-driven-development` (workstreams A/B in parallel + C inline) |
| 331 | [331-edit-documents-batch-422-fix.md](documents/331-edit-documents-batch-422-fix.md) | COMPLETED — 2026-05-12 | Sanitize `/webhook/edit-documents` Airtable batch before PATCH: drop records with non-`recXXX` id or all-undefined fields, log as VALIDATION via `logError`. Prevents one malformed Tally entry (e.g. `status_changes` with `new_status: undefined`) from 422ing the whole 10-record chunk. Pure helper in `api/src/lib/batch-sanitize.mjs` + 7 `node --test` cases (no vitest). Surfaces secondary bug: `error-logger.ts:40` swaps AirtableClient args (`(PAT, BASE_ID)` vs constructor `(baseId, pat)`) — silent security_logs write failure, flagged for 1-line follow-up DL |
| 330 | [330-ai-review-3pane-rework.md](ai-review/330-ai-review-3pane-rework.md) | COMPLETED — 2026-05-12 | AI Review tab reworked from 2-pane (sticky preview + single scrollable accordion column) to 3-pane master-detail (clients / docs-of-selected-client / preview). Each pane owns its scroll (`overscroll-behavior: contain`), widths 240/340/flex on ≥1200px. Client rows reuse `.ai-accordion-header` markup + new "X/Y reviewed" per-client counter; summary bar extended to "N pending · M clients · X/Y reviewed". Accordion click handler retired; `selectClient(name)` renders pane 2 + resets pane 3. Mobile modal untouched. Addresses "two scroll areas competing" + "long card list feels endless" |
| 329 | [329-preview-timeout-error-ux.md](admin-ui/329-preview-timeout-error-ux.md) | COMPLETED | Preview URL timeout 10s→20s; `humanizeError()` replaces raw "signal timed out" with Hebrew copy across all showModal call sites (12 total); inline retry button in preview panel on TimeoutError only |
| 322 | [322-note-save-silent-failures.md](infrastructure/322-note-save-silent-failures.md) | COMPLETED — 2026-05-12 | Instrument `summarizeAndSaveNote` (processor.ts:292-404) 4 silent-exit paths (dedup, body_too_short, llm_skip, exception). New `event_type: INBOUND_NOTE_SKIPPED` (severity INFO via existing `logSecurity`) for the 3 by-design skips; `logError` (INTERNAL) for catch path. JSON details `{reason, message_id, report_id, client_id}`. No PII in details. Zero behavior change — observability-only. Triggered by coralhouse2@gmail.com 2026-04-21 note that matched but never saved |
| 321 | [321-ai-review-perf-bundle.md](admin-ui/321-ai-review-perf-bundle.md) | COMPLETED — 2026-05-12 | AI Review endpoint perf bundle: scope DOCUMENTS fetch to reports in pending-classifications (N+1 fix), memoize buildShortName per-request, delete dead loadAIReviewCount, widen dedup cache to 3 s post-resolve, add idle-refresh dialog helper (5 min hidden + re-focus, respects open modals/inputs). Expected cold p50: 2–4 s (vs. 14–17 s baseline). DL-318 (response-cache) reverted; this DL avoids that approach |
| 321 | [321-classifier-non-document-verdict.md](ai-review/321-classifier-non-document-verdict.md) | COMPLETED — 2026-05-12 | Classifier can explicitly declare an inbound attachment is NOT a document (decorative header/logo image, email signature that escaped DL-305's 50KB threshold, or blank scanned page) so the processor short-circuits before OneDrive upload + `pending_classifications` insert. New required tool-schema fields `is_document: boolean` + `non_document_reason: enum('decorative','signature','blank_page','not_applicable')` on `classify_document` (Haiku 4.5 strict mode). `ClassificationResult` gains `isDocument?` + `nonDocumentReason?`. System prompt gains "NON-DOCUMENT detection (DL-321)" section with explicit bias toward `is_document=true` when in doubt. Guardrails: image-only short-circuit (PDFs always reach review), confidence ≥ 0.8 floor (low-conf non-doc verdicts fall through), missing field defaults to `true`. Observability: `console.warn [inbound][DL-321]` per drop (no error-log spam). Triggered by a live decorative `ATT00001.png` (62KB) slipping past DL-305 2026-04-20 and being manually rejected 2026-04-22 |
| 320 | [320-also-match-ux-rework.md](ai-review/320-also-match-ux-rework.md) | COMPLETED — 2026-05-12 | DL-314 "Also Matches" UX rework + decorative icon cleanup. Remove pre-approve "[H:hebrew].." button (all card states) — multi-match entry point moves to the reviewed-approved card as new **"[H:hebrew]"** button next to "[H:hebrew]", reusing `showAIAlsoMatchModal` as-is. Remove decorative "?" robot help icon (`.ai-evidence-trigger`) from all AI Review cards per NN/G tooltip guidelines. New backend action `action='revert_cascade'` on `/review-classification` + confirmation dialog on "[H:hebrew]" with count + sibling titles when `shared_ref_count > 1`; cascade clears primary + siblings and archives OneDrive file (reuses DL-314 `isLastReference`). `/get-pending-classifications` enriched with `shared_ref_count` / `shared_with_titles[]` / `shared_record_ids[]`; `onedrive_item_id` added to docRecords fetch and cache key bumped to `v2`. script.js cache v=275 → v=276. Resolves DL-314 §8 open TODO |
| 319 | [319-approve-creates-required-doc.md](ai-review/319-approve-creates-required-doc.md) | COMPLETED — 2026-05-12 | Flip DL-057 disabled "[H:hebrew]" button on AI Review full-match + fuzzy-match cards into active "[H:hebrew] - [H:add-doc] [H:hebrew]" when `item.is_unrequested && item.matched_template_id`. New wrapper `approveAIClassificationAddRequired(id, templateId)` POSTs `create_if_missing:true` + `template_id` to `/webhook/review-classification`; backend approve action creates a minimal `Required_Missing` DOCUMENTS row (server-side `matched_template_id` wins, typecast:true, race-check before create) then runs the existing approve flow unchanged. Unmatched-file fallback (no `matched_template_id`) preserves DL-057 disabled state. Mobile preview + issuer-mismatch unchanged |
| 318 | [318-ai-review-endpoint-perf.md](admin-ui/318-ai-review-endpoint-perf.md) | COMPLETED — 2026-05-12 | KV response-level cache (60 s TTL) for `/webhook/get-pending-classifications` keyed by `filing_type` (3 keys: `annual_report`, `capital_statements`, `all`); explicit invalidation on writes (3 sites: `also_match`, `review-classification`, processor classifier); drop dead field `email_body_text`; cache MS Graph webURL resolutions in KV (1 h TTL, checked in parallel before batch call). Target: p50 < 1.5 s warm, p95 < 3 s warm, zero `TimeoutError` on 10 consecutive AI Review tab clicks. |
| 317 | [317-fetch-only-prefetch.md](admin-ui/317-fetch-only-prefetch.md) | COMPLETED — 2026-05-12 | Split FETCH from RENDER for 5 heavy tab loaders (`loadPendingClients`, `loadAIClassifications`, `loadPendingApprovalQueue`, `loadReminders`, `loadQuestionnaires`). Prefetch pipeline now warms data cache + updates cheap badges/stats; heavy table/card render deferred to first tab click via per-loader `*EverRendered` flag. Each loader gains `prefetchOnly` param; 5 loaders re-added to `postBg`-staggered prefetch chain. `loadAIReviewCount` removed from pipeline (redundant with `loadAIClassifications(true, true)` hitting same endpoint via dedup). New perf marks `dl317:<name>:fetch` + `dl317:<name>:render`. Cache-bust v=272 → v=273 |
| 316 | [316-react-port-scoping.md](ai-review/316-react-port-scoping.md) | COMPLETED — 2026-05-12 | Scoping-only decision doc for porting AI Review tab from vanilla `script.js` (~3,500 LOC) to a React island following DL-306 precedent. Research-backed cost estimate: ~5 weeks / 1 dev for full-tab port (scaffold, master list, card rendering, approve/reassign/reject, split modal, preview, Vitest, event-bus to doc-manager, cutover). Recommendation: **don't port today** — Strangler Fig, wait for forcing function (next non-trivial AI Review feature or recurring bug). Reassess 2026-10-21 if no trigger fires. File:line anchors for all 6 flow groups inventoried (master list, card rendering, inline actions, PDF preview, split, reassign) + shared-helper ownership (`createDocCombobox` also powers DL-292). No code written |
| 315 | [315-pre-questionnaire-classifier-fallback.md](ai-review/315-pre-questionnaire-classifier-fallback.md) | COMPLETED — 2026-05-12 | Classifier fallback for docs arriving before client submits Tally questionnaire. Processor's `if (requiredDocs.length > 0)` guard at `processor.ts:788` dropped; new `fallbackMode` + `filingType` options on `classifyAttachment` swap in full filing-type-scoped `ALL_TEMPLATE_IDS` enum and rewrite the system-prompt "only match required" rule. `findBestDocMatch` + recovery agent skipped in fallback mode. New Airtable `pre_questionnaire` checkbox on `pending_classifications` (field id `flduTUbhFFqdI2qzi`); `ClassificationResult.preQuestionnaire` propagates through processor → field write → `classifications.ts` GET → new `.ai-pre-questionnaire-badge` on AI Review card (warning tone, `[H:hebrew]`). Trigger: `requiredDocs.length === 0` OR stage ∈ {Send_Questionnaire, Waiting_For_Answers}. Doc-manager badge scoped out (fallback classifications don't link to a `documents` row until office reassigns). DL-315 one-off `POST /webhook/backfill-dl315?clientId=CPA-XXX&dryRun=1` in `backfill.ts` replays classifier on historical null-template rows, writes matched_template + `pre_questionnaire: true` via `airtable.updateRecord` |
| 314 | [314-svg-sprite-icons.md](admin-ui/314-svg-sprite-icons.md) | COMPLETED — 2026-05-12 | Replace Lucide runtime DOM-replacement with inline SVG sprite (admin panel only). DL-311 profiling proved `safeCreateIcons:full-doc` (100–166ms each) was the remaining bottleneck after surgical fixes — every Chrome `setTimeout >700ms` violation was the browser bundling several Lucide DOM walks. Solution: 86-icon sprite at `frontend/assets/icons/icons.svg` inlined into `admin/index.html`; new `icon(name, sizeClass)` helper for dynamic templates; 136 static `<i data-lucide>` + 175 template-literal occurrences swapped to `<svg><use href="#icon-NAME"/></svg>`; `safeCreateIcons` neutered to no-op shim; Lucide CDN `<script>` removed. Build script `scripts/build-icon-sprite.mjs` is npm-free (native fetch to unpkg). Client portal / doc-manager / view-documents keep Lucide runtime — out of scope |
| 314 | [314-multi-template-match.md](ai-review/314-multi-template-match.md) | COMPLETED — 2026-05-12 | Multi-template match in AI Review — admin multi-match picker on card creates N doc records sharing one `onedrive_item_id`. No file duplication, no schema change. Reference-count gate on all archive call sites (approve/reassign override, reject, edit-documents revert). Cross-person + cross-filing (AR↔CS) allowed. V1 aborts on any target conflict. `/get-client-documents` surfaces `shared_ref_count` + `shared_with_titles[]` → ref chip in doc-manager |
| 313 | [313-hover-tab-dropdowns.md](admin-ui/313-hover-tab-dropdowns.md) | COMPLETED (live 2026-04-20) | Admin top-nav [Questionnaires] / [Reviews] dropdowns open on hover (desktop mouse only, gated by `@media (hover: hover) and (pointer: fine)`) with 180ms fade + translateY(-6px→0) cubic-bezier ease-out and `transform-origin: top`. Click-toggle preserved for keyboard/touch; `aria-expanded` sync via new `openTabDropdown`/`closeTabDropdown` helpers. 200ms close-delay timer (per-wrapper) bridges the trigger→menu gap; sibling auto-close on hover. Mobile bottom-nav popovers untouched. `prefers-reduced-motion` fallback |
| 311 | [311-admin-panel-slowness.md](admin-ui/311-admin-panel-slowness.md) | COMPLETED — 2026-05-12 | Admin panel long-task audit — switchTab unconditionally reloads dashboard + `STALE_AFTER_MS=30s` triggers repeat full renders; `renderClientsTable` runs two loops (table + mobile cards); `safeCreateIcons()` called unscoped on full DOM post-render; 7 prefetch loaders bundled in one `requestIdleCallback`. Plan: Part A adds `window.__ADMIN_PERF__`-gated `performance.mark/measure`; Part B applies 6 surgical fixes (remove `loadDashboard` from non-dashboard tab switch, bump staleness to 5min, merge render loops + `scheduler.yield` chunking, scope icons, stagger prefetch pump, debounce `switchTab`). script.js-only, no module split (DL-132) |
| 310 | [310-remove-questionnaire-answer-note.md](infrastructure/310-remove-questionnaire-answer-note.md) | COMPLETED — 2026-05-12 | Remove `[[H:hebrew]] <raw>` append to `bookkeepers_notes` (DL-296 feature). Endpoint keeps DL-300 ✨ `issuer_name_suggested` LLM path; note-append + `noteOnlyDocs` loop deleted. Backfill script strips historical tagged blocks from documents table (dry-run by default). WF02 payload left untouched — extra fields silently ignored |
| 309 | [309-silent-stage-advance-button.md](admin-ui/309-silent-stage-advance-button.md) | COMPLETED — 2026-05-12 | New silent-advance button on PA card footer + doc-manager sticky bar (sibling to primary approve-and-send). Advances stage 3 → Collecting_Docs via `ADMIN_CHANGE_STAGE` without sending the doc-request email. Doc-manager button gated by `STAGE_ORDER[CURRENT_STAGE] <= 3`; self-hides after advance. Icon `mail-off`, outline style, info-blue toast (not green) to avoid implying email was sent. Zero backend changes |
| 308 | [308-approve-send-email-preview.md](admin-ui/308-approve-send-email-preview.md) | COMPLETED (live 2026-04-20) | Read-only email preview modal before approve-and-send. New `?preview=1` dry-run flag on `/webhook/approve-and-send` returns `{subject, html, language}` without sending. New shared helper `frontend/shared/email-preview-modal.js` (DL-299 precedent) reuses DL-289 `.ai-modal-overlay` + iframe srcdoc pattern. Preview buttons in PA card action row and doc-manager header + sticky action bar. No CSS, no backend mutation — parity with real send guaranteed by reusing `buildClientEmail{Subject,Html}` |
| 307 | [306-preuploaded-docs-indicator-pa-and-dm.md](admin-ui/306-preuploaded-docs-indicator-pa-and-dm.md) | COMPLETED (live 2026-04-20) | PA card + doc-manager info banner when `pending_classifications` exist with `review_status='pending'`. Backend adds `pending_reviews_count` to `/admin-pending-approval` + `/get-client-reports`. Frontend: banner at top of `.pa-card__body` and below doc-manager `.page-header`; "[H:hebrew]AI Review" opens new tab. Non-blocking; stage=`Pending_Approval` only. Received docs show green ✓ icon in PA doc list. CSS uses `--info-*` tokens |
| 306 | [306-react-vite-ts-first-slice.md](admin-ui/306-react-vite-ts-first-slice.md) | COMPLETED (live 2026-04-20) | **Learning + system task.** Introduce Vite + React 18 + TS strict + TanStack Query v5 + Vitest via a single island (client detail modal). Unlocks DL-132's "bundler prerequisite" for splitting script.js. Coexists as React island inside existing admin HTML; preserves `openClientDetailModalShared` API via bridge shim; reuses design-system CSS + `window.showConfirmDialog`. Built output committed to `frontend/admin/react-dist/` |
| 305 | [305-inline-signature-filter-threshold.md](infrastructure/305-inline-signature-filter-threshold.md) | COMPLETED — 2026-05-12 | Raise `isInline` image size threshold 20KB → 50KB — DL-303's 20KB cap missed Outlook-rendered signature logos (~30KB); `image002.png` (29.9KB) was ingested as a client document (false positive) |
| 305 | [304-stage3-card-filter-and-refresh.md](admin-ui/304-stage3-card-filter-and-refresh.md) | COMPLETED — 2026-05-12 | Dashboard stage-3 stat card (received-questionnaire / docs-not-sent) now filters in place via `toggleStageFilter('3')` instead of hijacking to the PA queue tab. `approveAndSendFromQueue` success path also advances the matching `clientsData` row from `Pending_Approval` → `Collecting_Docs`, recalcs stats, and re-applies the current stage filter — so the stage-3 count + filtered table update without a manual refresh. PA queue no longer auto-expands the first 3 cards on navigation (all collapsed by default). Pencil doc-label edit now shows raw `<b>...</b>` tags in the input and keeps them on save (was stripping + losing bold on display) |
| 304 | [303-inline-attachment-filter.md](infrastructure/303-inline-attachment-filter.md) | COMPLETED (live 2026-04-18) | Fix inbound pipeline silently dropping PDFs sent from iPhone Mail — `isInline` blanket filter replaced with narrow `isInline && IMAGE_EXTENSIONS && size < 20KB` guard that preserves signature-logo rejection while letting inline PDFs/docs through |
| 303 | [301-pa-queue-search-bar.md](admin-ui/301-pa-queue-search-bar.md) | COMPLETED — 2026-05-12 | Client-side search input in PA queue filter bar — matches name, email, spouse; debounced 150ms; clear-X button; no-match vs truly-empty state distinction; composes with server-side year/filing-type filters |
| 302 | [302-pa-card-hover-cross-reference.md](admin-ui/302-pa-card-hover-cross-reference.md) | COMPLETED (live 2026-04-18) | PA card answer↔doc cross-highlight (brushing & linking). Backend joins `question_mappings` (tblWr2sK1YvyLWG3X, KV-cached 1h) and attaches `template_ids[]` to each answer in `admin-pending-approval`. Frontend: `data-template-ids` on `.pa-preview-qa-row`, `data-template-id` (`d.type`) on `.pa-preview-doc-row`, `tabindex=0`. New `_paLink*` module: hover/focus apply `.pa-link-highlight` (tinted bg + 3px start-edge bar); coarse-pointer = tap-to-pin / outside-tap clears. Orphan docs get `title="[H:hebrew]"` + dashed outline. v1 covers free-text answers only (yes/no chips not rendered per DL-299) |
| 301 | [301-pa-add-doc-affordance.md](admin-ui/301-pa-add-doc-affordance.md) | COMPLETED — 2026-05-12 | PA card: inline "+ [H:add-doc]" row per person group. Popover reuses doc-manager's template catalog (via `GET_CLIENT_DOCUMENTS`), with variables step + preview, plus free-text custom doc (`general_doc`). Spouse/client toggle when `item.spouse_name`. Immediate save via `EDIT_DOCUMENTS` `docs_to_create` (status `Required_Missing`), optimistic local update + rollback on error. Duplicate guard on `(template_id, issuer_key)`. Report stays in `Pending_Approval`. No backend changes |
| 300 | [300-per-template-issuer-suggestion-gate.md](infrastructure/300-per-template-issuer-suggestion-gate.md) | COMPLETED — 2026-05-12 | Per-template gate for WF02 ✨ issuer extraction: new Airtable `needs_issuer_suggestion` checkbox on templates; `/webhook/extract-issuer-names` loads templates, partitions docs, only calls Haiku for opted-in templates (default false = opt-in). Raw-context `bookkeepers_notes` append still runs for all docs. One-shot cleanup script clears `issuer_name_suggested` on disabled-template docs. **Follow-up:** ✨ accept chip hidden on PA card + doc-manager (commit `ca3e7d5`) — accept clobbered doc-row label with bare issuer; backend writes still run for opted-in templates |
| 299 | [299-pa-card-issuer-edit-notes-print.md](admin-ui/299-pa-card-issuer-edit-notes-print.md) | COMPLETED — 2026-05-12 | PA card parity w/ doc-manager: (1) per-doc pencil → inline issuer edit + `[H:hebrew]` combobox for T501/T401/T301 (from `company_links`); (2) per-doc note popover — immediate save of `bookkeepers_notes`; (3) questionnaire print button in Q&A section title. Shared `frontend/shared/print-questionnaire.js` replaces doc-manager inline print. No new endpoints |
| 298 | [298-pa-queue-stacked-cards.md](admin-ui/298-pa-queue-stacked-cards.md) | COMPLETED — 2026-05-12 | PA queue redesign: delete master/preview split; stacked full-width cards (`.pa-stack`) with collapsed header (name, age, count badges, folder-open doc-manager link, chevron) + expandable body (DL-295 2-col Q&A \| Docs, notes, questions, actions). First 3 of each page auto-expanded. DL-296 ✨ issuer suggestion chip moves inline into each doc row. Deletes `loadPaPreview`, `loadPaMobilePreview`, `buildPaPreviewHtml`, `_activePaReportId`, `paMobilePreviewModal` |
| 297 | [297-doc-manager-header-sticky-stage-edit.md](admin-ui/297-doc-manager-header-sticky-stage-edit.md) | COMPLETED — 2026-05-12 | Doc-manager polish: sticky action bar moved above page-header block (position:sticky instead of fixed, visually reads as top row of header when at-top, pins on scroll). Stage label (`[H:hebrew]`) now clickable → dropdown with 8 stages, reuses `ADMIN_CHANGE_STAGE` endpoint with optimistic update + revert |
| 296 | [296-wf02-extract-issuer-names.md](infrastructure/296-wf02-extract-issuer-names.md) | COMPLETED — 2026-05-12 | WF02 calls new `/webhook/extract-issuer-names` (Haiku 4.5 + forced tool use) to extract issuer names from questionnaire free-text; stores as `issuer_name_suggested`; Review & Approve card shows bold ✨ chip with 1-click accept (+ "[H:hebrew]" batch); raw context preserved in `bookkeepers_notes`. **Follow-up (DL-300):** ✨ accept chip hidden on frontend — accept clobbered full doc-row label; backend writes still run |
| 295 | [295-pa-queue-improvements.md](admin-ui/295-pa-queue-improvements.md) | COMPLETED — 2026-05-12 | PA queue: 2-column preview (Q&A \| docs) on desktop, stacked on mobile; fix `{placeholder}` leak by flattening `doc_chips` from `doc_groups`; age priority badge on master cards (red >7d / yellow 3–7d); inline doc status menu in preview reusing DL-227 pattern |
| 294 | [294-pa-queue-redesign.md](admin-ui/294-pa-queue-redesign.md) | COMPLETED — 2026-05-12 | PA queue redesign: render `<b>issuer</b>` via `renderDocLabel`; split `doc_chips` (short_name) from `doc_groups` (per-person/per-category); redesigned preview panel with sticky client header + stats strip + sticky footer CTA + Yes/free-text answer grouping |
| 293 | [293-doc-manager-edit-client.md](admin-ui/293-doc-manager-edit-client.md) | COMPLETED — 2026-05-12 | Doc-manager pencil icon + inline email/cc_email/phone editing. Extracts DL-106 modal into shared `client-detail-modal.js` reused by dashboard + doc-manager; adds `client_phone` to GET_CLIENT_REPORTS office response. Supersedes DL-107 |
| 292 | [292-pending-approval-review-queue-tab.md](admin-ui/292-pending-approval-review-queue-tab.md) | COMPLETED — 2026-05-12 | New "[H:hebrew]" tab — stage-3 (Pending_Approval) queue with AI-Review-style split view (master cards + sticky preview); reuses `APPROVE_AND_SEND` + `EDIT_DOCUMENTS`; addresses DL-291 W-1 scroll friction |
| 290 | [290-pending-classification-count-mismatch.md](admin-ui/290-pending-classification-count-mismatch.md) | COMPLETED — 2026-05-12 | Align "[H:hebrew]" reminder stat with AI Review badge — `reminders.ts` `pending_review` now counts unique `client_id` (any stage in reminder scope) instead of reports in Collecting_Docs only; fixes dual-filing-type double-count |
| 289 | [289-recent-messages-checkmark-thread.md](admin-ui/289-recent-messages-checkmark-thread.md) | COMPLETED — 2026-05-12 | Recent messages panel: stack 2+ office reply threads (was single-reply overwrite), replace trash icon with check (mark as handled = soft hide), inline post-reply prompt appended below row, Gmail-style expand-compose modal with live email preview iframe |
| 288 | [288-queued-subtitle-no-stale-flash.md](admin-ui/288-queued-subtitle-no-stale-flash.md) | COMPLETED — 2026-05-12 | Fix stale-flash of `(N [H:hebrew])` subtitle on dashboard load — remove DL-273 §8 `queued_send_at` fallback in `recalculateStats()`; render only after Outbox fetch resolves |
| 287 | [287-cloudflare-queues-inbound-email.md](infrastructure/287-cloudflare-queues-inbound-email.md) | COMPLETED — 2026-05-12 | Cloudflare Queues migration for inbound email — decouples HTTP lifecycle from processing; producer enqueues + 202, consumer runs dedup lock + processInboundEmail with full 5min CPU budget; DLQ + feature flag `USE_QUEUE`; classifier serialized (BATCH_SIZE 3→1) |
| 284 | [284-admin-questionnaire-link-on-behalf.md](admin-ui/284-admin-questionnaire-link-on-behalf.md) | COMPLETED — 2026-05-12 | Admin right-click menu item — open questionnaire landing page on behalf of client (assisted mode); 24h token + audit log + visible banner |
| 283 | [283-n8n-workflow-errors-investigation.md](infrastructure/283-n8n-workflow-errors-investigation.md) | SUPERSEDED by DL-287 | n8n morning errors: WF02/WF06 Airtable 401 (missed credential in Session 14 PAT rotation) + WF05 Worker 120s timeout (decouple via ctx.waitUntil) + PAT rotation runbook |
| 282 | [282-forwarded-email-note-recurrence.md](email/282-forwarded-email-note-recurrence.md) | COMPLETED — 2026-05-12 | Fix forwarded-email misattribution (DL-279 recurrence): reorder identifier cascade for office senders who are themselves clients, match-method-aware note sender resolver, strip first-line forward headers, extend parseForwardedEmail + backfill sweep |
| 281 | [281-queued-emails-outbox-source-of-truth.md](email/281-queued-emails-outbox-source-of-truth.md) | COMPLETED — 2026-05-12 | Queue view modal + Outlook Outbox as source of truth for pending deferred sends (fixes DL-273 §8 staleness gap) |
| 280 | [280-fix-mobile-bottom-nav-hidden.md](admin-ui/280-fix-mobile-bottom-nav-hidden.md) | COMPLETED — 2026-05-12 | Mobile bottom nav hidden after login. v1 (JS-clears-inline-style) regressed via DL-281 merge. v2 root fix: class-based FOUC gate (`.fouc-hidden` ↔ `.visible`) + chat widget audit (sibling-combinator → class) |
| 278 | [278-ai-review-client-list-layout.md](ai-review/278-ai-review-client-list-layout.md) | COMPLETED — 2026-05-12 | AI review: viewport-locked split layout with independent scrolling master panel |
| 277 | [277-fix-reminder-progress-bar-and-429-retry.md](email/277-fix-reminder-progress-bar-and-429-retry.md) | COMPLETED — 2026-05-12 | Fix reminder progress bar math (waived excluded) + 429 retry with backoff + re-classify endpoint |
| 276 | [276-smooth-admin-auth-flow.md](admin-ui/276-smooth-admin-auth-flow.md) | COMPLETED — 2026-05-12 | Smooth admin auth: splash screen + parallel prefetch eliminates login screen flash |
| 275 | [275-fix-zero-docs-stage-stuck.md](infrastructure/275-fix-zero-docs-stage-stuck.md) | COMPLETED | Fix 0-doc questionnaires stuck at Waiting_For_Answers — restructure WF02 merge node + backfill 6 reports |
| 274 | [274-dashboard-messages-search.md](admin-ui/274-dashboard-messages-search.md) | COMPLETED | Dashboard messages: search bar across all years with fetch-once client-side filtering |
| 273 | [273-outlook-deferred-send.md](email/273-outlook-deferred-send.md) | COMPLETED — 2026-05-12 | Replace KV+cron email queue with MS Graph PidTagDeferredSendTime deferred delivery |
| 271 | [271-reminder-06am-and-pending-filter-bug.md](reminders/271-reminder-06am-and-pending-filter-bug.md) | COMPLETED — 2026-05-12 | Fix reminder 06 AM timing (cron→08:00), pending classification filter bypass, monthly reset credential |
| 272 | [272-dashboard-messages-load-more.md](admin-ui/272-dashboard-messages-load-more.md) | COMPLETED | Dashboard messages: "load more" client-side pagination + same-day sort fix |
| 270 | [270-editable-contract-period-dates.md](ai-review/270-editable-contract-period-dates.md) | COMPLETED | Editable contract period dates on AI review card for T901/T902 (DL-269 refinement) |
| 266 | [266-reply-to-client-messages.md](admin-ui/266-reply-to-client-messages.md) | COMPLETED — 2026-05-12 | Reply to client messages from dashboard panel — branded email + off-hours queue |
| 269 | [269-partial-rental-contract-detection.md](ai-review/269-partial-rental-contract-detection.md) | COMPLETED | AI detects partial rental contracts (T901/T902), banner on review card to request missing period |
| 268 | [268-ai-review-pagination.md](ai-review/268-ai-review-pagination.md) | COMPLETED — 2026-05-12 | AI review: paginate by client groups (25/page) + FIFO sort (oldest-waiting first) |
| 267 | [267-auto-advance-zero-docs-to-review.md](admin-ui/267-auto-advance-zero-docs-to-review.md) | COMPLETED — 2026-05-12 | Auto-advance to Review when docs_missing_count reaches 0 (Pending_Approval or Collecting_Docs) |
| 265 | [265-entity-tab-switch-loading.md](admin-ui/265-entity-tab-switch-loading.md) | COMPLETED — 2026-05-12 | Entity tab switch: inline spinner + opacity fade for all data tabs including dashboard |
| 264 | [264-off-hours-email-queue.md](email/264-off-hours-email-queue.md) | COMPLETED — 2026-05-12 | Off-hours (8PM-8AM) approve-and-send emails queued for 8AM morning delivery |
| 263 | [263-dashboard-messages-delete-and-raw-text.md](admin-ui/263-dashboard-messages-delete-and-raw-text.md) | COMPLETED — 2026-05-12 | Dashboard messages: delete/hide option + raw text instead of AI summary |
| 260 | [260-archive-extraction-inbound-email.md](documents/260-archive-extraction-inbound-email.md) | COMPLETED — 2026-05-12 | Auto-extract ZIP/RAR/7z archives in inbound email pipeline |
| 259 | [259-inbound-notes-all-stages.md](infrastructure/259-inbound-notes-all-stages.md) | COMPLETED — 2026-05-12 | Capture client notes & raw attachments at all stages, not just Collecting_Docs/Review |
| 258 | [258-client-messages-low-stages.md](admin-ui/258-client-messages-low-stages.md) | COMPLETED — 2026-05-12 | Show notes & client messages on low-stage (≤1) doc manager by extracting secondary zone from #content |
| 257 | [257-reminder-select-all-bug-and-cap.md](reminders/257-reminder-select-all-bug-and-cap.md) | COMPLETED — 2026-05-12 | Fix reminder select-all double-counting (table+mobile duplicate checkboxes) + add MAX_BULK_SEND=50 cap |
| 256 | [256-table-pagination.md](admin-ui/256-table-pagination.md) | COMPLETED — 2026-05-12 | Table pagination: 50 rows/page across all tabs, shared renderPagination utility, scoped safeCreateIcons |
| 255 | [255-table-rendering-performance.md](admin-ui/255-table-rendering-performance.md) | SUPERSEDED by DL-256 | Table rendering: hide/show filtering, 150ms debounce, CSS content-visibility |
| 254 | [254-dashboard-load-performance.md](admin-ui/254-dashboard-load-performance.md) | COMPLETED — 2026-05-12 | Dashboard load: fix double-load, stagger prefetches, KV cache years+docs, parallel batches |
| 253 | [253-rejected-uploads-group-by-reason.md](email/253-rejected-uploads-group-by-reason.md) | COMPLETED | Group rejected uploads by reason in email callout (DL-244 refinement) |
| 251 | [251-view-documents-filing-type-badge.md](client-portal/251-view-documents-filing-type-badge.md) | COMPLETED | View Documents — filing type badge in header for dual AR+CS clients |
| 250 | [250-entity-tab-switch-dashboard-reload.md](admin-ui/250-entity-tab-switch-dashboard-reload.md) | COMPLETED | Fix: entity tab switch (AR/CS) — dashboard reload, import opacity fade, filing type badge |
| 252 | [252-frontend-orchestrated-split.md](ai-review/252-frontend-orchestrated-split.md) | COMPLETED — 2026-05-12 | Frontend-orchestrated split with live progress UI, per-segment classification, OneDrive rename |
| 249 | [249-safe-split-rollback.md](ai-review/249-safe-split-rollback.md) | SUPERSEDED by DL-252 | Safe split: status transition + rollback on failure instead of delete-before-process |
| 248 | [248-reassign-clears-unrelated-approval.md](ai-review/248-reassign-clears-unrelated-approval.md) | COMPLETED — 2026-05-12 | Fix: reassign blind-clear undoes unrelated approval on same source doc |
| 247 | [247-tab-switching-performance.md](admin-ui/247-tab-switching-performance.md) | COMPLETED — 2026-05-12 | Tab switching SWR: remove full-screen overlay from tab navigation, prefetch AI review, deduplicatedFetch, staleness-based refresh |
| 246 | [246-split-modal-page-preview-zoom.md](admin-ui/246-split-modal-page-preview-zoom.md) | COMPLETED | PDF split modal page preview lightbox with zoom/pan, keyboard nav, hover magnify icon |
| 245 | [245-agentic-classification-workflows.md](ai-review/245-agentic-classification-workflows.md) | COMPLETED — 2026-05-12 | Multi-agent workflows for WF05: PDF Splitter (Haiku), Critic (Sonnet evaluator-optimizer), Financial Extractor — gated by cheap pre-checks, runs in ctx.waitUntil() |
| 244 | [244-rejected-uploads-visibility.md](documents/244-rejected-uploads-visibility.md) | COMPLETED — 2026-05-12 | Rejected uploads visibility — log on report record, callout in approve-and-send + Type B reminder + portal + admin doc-manager (delete only). Reject flow logs filename+date+reason; auto-clears past Collecting_Docs |
| 243 | [243-cs-help-text-content.md](capital-statements/243-cs-help-text-content.md) | COMPLETED — 2026-05-12 | CS view-documents `?` help text — 16/22 templates populated (help_he + help_en), KV cache purged |
| 242 | [242-questionnaires-tab-print-notes-questions.md](admin-ui/242-questionnaires-tab-print-notes-questions.md) | COMPLETED | Fix questionnaires-tab print: client questions + office notes missing (API now returns notes per item) |
| 241 | [241-cs-template-short-names.md](capital-statements/241-cs-template-short-names.md) | COMPLETED — 2026-05-12 | CS template short_name_he — add issuer placeholders so reassign combobox shows per-issuer names |
| 240 | [240-remove-onedrive-subfolders.md](documents/240-remove-onedrive-subfolders.md) | COMPLETED — 2026-05-12 | Remove [H:hebrew]/[H:hebrew] subfolders — all docs go to filing type root |
| 239 | [239-cross-filing-type-reassign.md](ai-review/239-cross-filing-type-reassign.md) | COMPLETED — 2026-05-12 | Cross-filing-type reassign — toggle between AR/CS doc lists in reassign modal |
| 235 | [235-onedrive-folder-routing-restructure.md](documents/235-onedrive-folder-routing-restructure.md) | COMPLETED — 2026-05-12 | OneDrive folder restructure: singular names, [H:hebrew] at year level |
| 234 | [234-skip-own-outbound-emails.md](infrastructure/234-skip-own-outbound-emails.md) | COMPLETED — 2026-05-12 | Skip emails from reports@ in inbound pipeline to prevent self-loop client notes |
| 233 | [233-cs-template-fixes.md](capital-statements/233-cs-template-fixes.md) | COMPLETED — 2026-05-12 | CS doc fixes: strip ** from year, compute year_plus_1, questionnaire for CS type |
| 238 | [238-unified-ai-review-both-filing-types.md](ai-review/238-unified-ai-review-both-filing-types.md) | COMPLETED — 2026-05-12 | Unified AI Review tab — show both AR & CS classifications with filing type badges |
| 237 | [237-pdf-split-reclassify.md](ai-review/237-pdf-split-reclassify.md) | COMPLETED — 2026-05-12 | PDF split & re-classify from AI review — split multi-page PDFs, reclassify each segment |
| 236 | [236-bulk-send-cap-50.md](admin-ui/236-bulk-send-cap-50.md) | COMPLETED — 2026-05-12 | Cap bulk questionnaire sending to 50 per batch |
| 232 | [232-email-print-filing-type-complete-audit.md](email/232-email-print-filing-type-complete-audit.md) | COMPLETED — 2026-05-12 | Complete email + print filing type audit: fix reminders, doc request, print, WhatsApp |
| 231 | [231-keep-both-missing-document-keys.md](ai-review/231-keep-both-missing-document-keys.md) | COMPLETED — 2026-05-12 | Fix keep_both missing document_key, document_uid, issuer_key |
| 230 | [230-duplicate-classification-missing-file-info.md](infrastructure/230-duplicate-classification-missing-file-info.md) | COMPLETED — 2026-05-12 | Fix duplicate classifications missing file_url and onedrive_item_id |
| 229 | [229-ecc-library-analysis.md](infrastructure/229-ecc-library-analysis.md) | COMPLETED | ECC library analysis — hooks, skills, agents, contexts patterns for our setup |
| 228 | [228-smart-add-second-filing-type.md](admin-ui/228-smart-add-second-filing-type.md) | COMPLETED — 2026-05-12 | Smart add second filing type: email blur pre-fill, row menu shortcut, doc manager button, tab linking |
| 227 | [227-inline-waive-receive-doc-tags.md](ai-review/227-inline-waive-receive-doc-tags.md) | COMPLETED — 2026-05-12 | Inline waive/receive on AI Review doc tags — click to toggle, hover for receive |
| 226 | [226-dual-filing-classification-onedrive.md](infrastructure/226-dual-filing-classification-onedrive.md) | COMPLETED — 2026-05-12 | Dual-filing classification + OneDrive folder architecture ([H:hebrew]/[H:hebrew]) |
| 225 | [225-cs-hardcoded-ar-remediation.md](capital-statements/225-cs-hardcoded-ar-remediation.md) | COMPLETED | CS hardcoded AR remediation — dynamic filing type labels across n8n, frontend, API |
| 222c | [222-multi-pdf-approve-conflict.md](ai-review/222-multi-pdf-approve-conflict.md) | COMPLETED — 2026-05-12 | Multi-PDF approve conflict: merge / keep both / override |
| 222b | [222-fix-document-manager-report-id-links.md](admin-ui/222-fix-document-manager-report-id-links.md) | COMPLETED — 2026-05-12 | Fix document-manager links: report_id → client_id |
| 223 | [223-backfill-filing-type-empty-records.md](infrastructure/223-backfill-filing-type-empty-records.md) | COMPLETED | Backfill empty filing_type on 33 legacy report records |
| 216 | [216-filing-type-scoping-all-tabs.md](admin-ui/216-filing-type-scoping-all-tabs.md) | COMPLETED | Filing type scoping across all admin tabs + mobile navbar entity toggle |
| 214 | [214-mobile-table-card-layout.md](admin-ui/214-mobile-table-card-layout.md) | COMPLETED | Mobile table → card layout for all 5 admin tables + collapsible filter bar |
| 212 | [212-mobile-bottom-nav-ai-review.md](admin-ui/212-mobile-bottom-nav-ai-review.md) | COMPLETED | Mobile bottom nav bar + AI review full-screen preview modal |
| 211 | [context-audit.md](infrastructure/context-audit.md) | COMPLETED — 2026-05-12 | Context efficiency audit — files to delete/update, CLAUDE.md cleanup, token budget analysis |
| 210 | [210-classification-review-test-bugfixes.md](ai-review/210-classification-review-test-bugfixes.md) | COMPLETED — 2026-05-12 | Fix 4 classification review bugs + "[H:hebrew]" dismiss UI |
| 207 | [207-wf05-worker-full-gap-audit.md](infrastructure/207-wf05-worker-full-gap-audit.md) | COMPLETED — 2026-05-12 | **DEFINITIVE** — Full WF05 Worker gap audit: 17 gaps, 29 verified ✅, 6 improvements |
| 206 | [206-wf05-worker-classification-parity.md](infrastructure/206-wf05-worker-classification-parity.md) | SUPERSEDED by DL-207 | WF05 Worker classification prompt parity (partial audit) |
| 205 | [205-clear-file-fields-on-status-revert.md](documents/205-clear-file-fields-on-status-revert.md) | COMPLETED | Clear file fields when doc status reverts to Missing |
| 204 | [204-digest-claude-ai-summarization.md](email/204-digest-claude-ai-summarization.md) | COMPLETED | Daily digest — Claude AI inbox summarization + weekend skip |
| 203 | [203-wf05-worker-migration.md](infrastructure/203-wf05-worker-migration.md) | COMPLETED | WF05 inbound email processing — migrated from 56-node n8n to Worker endpoint |
| 202 | [202-daily-digest-incoming-emails-section.md](email/202-daily-digest-incoming-emails-section.md) | COMPLETED | Daily digest communication feed — MS Graph inbox query, sender + body preview |
| 201 | [201-fix-review-classification-422-email-validation.md](ai-review/201-fix-review-classification-422-email-validation.md) | COMPLETED | Fix review-classification 422 — sanitize email/null fields before Airtable PATCH |
| 199 | [199-client-communication-notes.md](admin-ui/199-client-communication-notes.md) | COMPLETED | Client communication notes — AI review tab, document manager timeline, WF05 auto-append |
| 197 | [197-fix-t501-short-name-audit-templates.md](ai-review/197-fix-t501-short-name-audit-templates.md) | COMPLETED | Fix T501 short name missing deposit type + audit 6 templates with literal bold pollution |
| 196 | [196-fix-empty-binary-upload-field-mismatch.md](ai-review/196-fix-empty-binary-upload-field-mismatch.md) | COMPLETED | Fix 0-byte uploads — field name mismatch in Process and Prepare Upload |
| 195 | [195-fix-tool-use-response-parsing.md](ai-review/195-fix-tool-use-response-parsing.md) | COMPLETED | Fix tool_use response parsing in WF05 classifier — all docs failing since DL-131 |
| 194 | [194-remove-batch-status-feature.md](ai-review/194-remove-batch-status-feature.md) | COMPLETED | Remove batch status feature — UI, API endpoint, n8n workflow |
| 192 | [192-fix-duplicate-life-insurance-documents.md](documents/192-fix-duplicate-life-insurance-documents.md) | COMPLETED — 2026-05-12 | Fix duplicate T501 life insurance docs when multiple Tally questions produce same deposit |
| 191 | [191-remove-delete-start-over-button.md](client-portal/191-remove-delete-start-over-button.md) | COMPLETED — 2026-05-12 | Remove "Delete & Start Over" button from client portal landing page |
| 190 | [190-questionnaire-toggle-hide-no-answers.md](client-portal/190-questionnaire-toggle-hide-no-answers.md) | COMPLETED | Toggle to hide "No" answers in questionnaire on-screen view |
| 189 | [189-add-phone-to-office-email-header.md](email/189-add-phone-to-office-email-header.md) | COMPLETED | Add phone number to office email summary box |
| 188 | [188-stop-email-move-show-body.md](ai-review/188-stop-email-move-show-body.md) | COMPLETED | Stop email folder move + show email body in AI review |
| 186 | [186-add-logo-all-emails.md](email/186-add-logo-all-emails.md) | COMPLETED | Add Moshe Atsits logo to all 7 email types |
| 199 | [199-wf05-code-node-http-blocked.md](ai-review/199-wf05-code-node-http-blocked.md) | BLOCKED — NEEDS RESEARCH | WF05 Code node HTTP calls ($helpers.httpRequest + fetch) silently fail on n8n Cloud |
| 200 | [200-document-manager-ux-improvements.md](admin-ui/200-document-manager-ux-improvements.md) | COMPLETED | Document Manager UX — 9 improvements across 3 phases |
| 187 | [187-stage3-attention-bounce.md](admin-ui/187-stage3-attention-bounce.md) | COMPLETED — 2026-05-12 | Stage 3 Attention Bounce Animation |
| 328 | [328-ai-review-ask-client-questions.md](admin-ui/328-ai-review-ask-client-questions.md) | COMPLETED — 2026-05-12 | AI Review — Ask Client Questions About Reviewed Batch |

---
*Last reorganized: 2026-03-25 — 157 logs across 10 domain folders*
